[package]
name    = "lwprog"
version = "0.1.0"
loft    = ">=0.8.5"
description = "repro: entry naming an ambiguous struct beside a library loop-write"
license = "LGPL-3.0-or-later"

[dependencies]
slotlib  = { path = "../slotlib" }
otherlib = { path = "../otherlib" }
