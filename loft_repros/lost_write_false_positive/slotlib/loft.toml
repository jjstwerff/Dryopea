[package]
name    = "slotlib"
version = "0.1.0"
loft    = ">=0.8.5"
description = "repro: the library whose loop-variable write is falsely called lost"
license = "LGPL-3.0-or-later"

[library]
entry = "src/slotlib.loft"
