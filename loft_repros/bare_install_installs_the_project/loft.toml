[package]
name    = "instprobe"
version = "0.1.0"
loft    = ">=0.8"

[library]
entry = "src/instprobe.loft"

[dependencies]
# a dependency that is NOT installed — the case `loft api` tells you to
# fix by running `loft install`
moros_map = { path = "../nowhere/moros_map" }
