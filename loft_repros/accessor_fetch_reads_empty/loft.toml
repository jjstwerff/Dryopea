[package]
name    = "accessor_fetch_reads_empty"
version = "0.0.1"
loft    = ">=0.8.5"

# ⚠ NO dependencies, deliberately: the first version of this reproducer
# used `mesh3d::Mesh` as the unrelated allocation, because that is where
# dryopea met it.  A plain local struct with a vector reproduces it
# identically, which is what makes the recipe a language-level one.
