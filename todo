Always:
Remember to always update the doc/claude/*.md files with the latest relevant findings.

Fix #index and introduce #next on iterating over strings.
- #index becomes the position of the current character.
- #next is the position of the cursor after the current character.
- fix the documentation too and the places in loft code where it is currently used.

Code generation:
- continue with the rust code generation by evaluating one tests at the time inside /generated/tests/
- per test that needed changes in the code generation evaluate the code quality according to /doc/claude/CODE.md

Start with a separate testing suite:
- When something fails in this suite the normal way of working is creating new tests in /tests/ to repair the problem with full debugging enabled.
- Move the relevant tests from /tests/ to this new suite that only contains .loft files.
- Create shorter files with clear categories.
- Include missing cases: for example writing and reading files with data types that are not yet tested.
- Make asserts that clearly show which test fails, it is not important to show any details outside what fails.
- Then create a Makefile target that runs all the defined tests in the suite.
- Do not generate /code/*.txt files or rust code during running this new suite. This is for performance and generally less stress on the tools.

Identify less important tests in /tests/ and let them only live inside the testing suite.


Evaluate if the output of /tests/code/*.txt files could be made more useful for debugging purposes.

Evaluate the ways untrusted loft code could compromise a system.

Evaluate how loft could trigger a runtime exception and write it in /doc/claude/QUALITY.md

Evaluate how the project can be made easier to develop against, especially the type system differences between the language and the database.


Check on #remove outside for..if?
More free polymorphism tests: meld of the two forms
Change the #index on a string iterator to hold the start of the current character.
- We probably need another 'hidden' index variable to make this work and only create the #index as an output variable.

Keep the "Format" class usable in user code. Even think about "File".
- Part of library name spacing?

Introduce a separate null string value.
- Validate in unit-tests that the empty string is not null.
- Think about what a null append to another string should do.

click on hex grid: assume vertical view
    x,y to correct hex position & relative position on the hex
    dragging for multiple hexes selection & rotate the camera
sampling png onto hex grid: specific point & 4 pixels around it
    hex center into exact position on the png-grid
    starting with rectangle that approximates the hex
    get the pixels with their share of the tile (potentially more than 4)
draw coast lines:
    walls at 15 degrees based on rough shape of hexes (step = 60, 2 steps = 30)
    start with middles of sides towards the sea
    path: (x,y) dir & steps (turn left/right)
    match longest pattern first (sets: round30, round15, circles3, circles6, flats30, flats15)
    points (x,y,z) triangles materials
general slopes (flats, gradual, hills, mountain)
water flow, amount of water
    random direction per tile
    breaking cicles from the coast with flipping
tree/plant growth
steep sides into rock faces
lakes as non-connected seas
roofs / walls / roads / rails (flatten)
editing height and open terrain
    show pre-build items & place them with position and rotation
    clicking including height and multiple layers
shortest path
    on single layer
    connections between layers
    width of room versus vehicle
    maximum slope (different materials)
    maximum water depth to pass (wading, amimals, vehicle)
    following water for boats (ocean, sea, river, lake)
