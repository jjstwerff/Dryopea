***** Permanent *****
Validate the doc/claude/*.md files for completeness and functionality for developing the application.
Optimize the doc/claude/*.md files for discoverability and efficiency. Cleanup the parts with almost no value.


** Session **
Follow the planning, state what you chose and why, validate the resulting changes against CODE.md and update all documentation with the resulting changes.
Follow the planning, try to create separate unit-tests to create clear cases, validate the resulting changes against CODE.md and update all documentation with the resulting changes.
Evaluate if better temporary logging, an optional internal analysis tool or boundary checking would help by investigating this issue.
Evaluate if better temporary logging, an optional internal analysis tool or boundary checking would have helped by investigating the last issue.

** Finally **
Update the doc/claude/*.md files with the latest relevant findings. Evaluate this with user documentation in tests/suite too.
Validate if everything that needs testing is in the tests/loft tests.

** Occationally **
Validate the project to the coding standards, try to remove any introduced clippy ignored annotations.
Validate pdf creation for problems.
Update the documentation in doc/*.html as generated via doc/suite/*.loft make it useful for entry level programmers with the explanation of key concepts with clear and inviting examples.
Identify less important tests in tests/*.rs files and let them only live inside the tests/loft tests.
Create a planning with enhancement of any kind that are documented ordered by how pressing they are, give slightly more priority to issues that might break programs.

Find spots where the runtime implementation or functions can be made more optimal, though weigh that versus the extra cost in terms of more code or maintainability. Document these results.
Find ways to make the full test run more efficient but still as useful.

Create unit-tests with multiple parameters of context on parallell dispatch function

Is there an obvious place to simplify the code with the same overall algorithm.

/btw show the status of the current task


investigate the versus rust conclusions and update the document with it
update the document with versus rust conclusions with the latest insights

report on possible runtime inefficiencies for the interpreter, both on processor cycles and memory consumption.
evaluate ways to get rid of the scratch buffer for strings for efficiency.
evaluate ways to get rid of the work variables inside loft that span the whole scope of efficiency.

most important clippy and rust warnings missing in loft

---- Code generation:
- continue with the rust code generation by evaluating one tests at the time inside /generated/tests/
- per test that needed changes in the code generation evaluate the code quality according to /doc/claude/CODE.md

Evaluate the ways untrusted loft code could compromise a system.

Evaluate how loft could trigger a runtime exception

Evaluate how the project can be made easier to develop against, especially the type system differences between the language and the database.

Check on #remove outside for..if?

Keep the "Format" class usable in user code. Even think about "File".
- Part of library name spacing?


click on hex grid: assume vertical view
    x,y to correct hex position & relative position on the hex
    dragging for multiple hexes selection & rotate the camera
sampling png onto hex grid: specific point & 4 pixels around it
    hex center into exact position on the png-grid
    starting with rectangle that approximates the hex
    get the pixels with their share of the tile (potentially more than 4)
draw coast lines:
    walls at 15 degrees based on rough shape of hexes (step = 60, 2 steps = 30)
    start with middles of sides towards the sea
    path: (x,y) dir & steps (turn left/right)
    match longest pattern first (sets: round30, round15, circles3, circles6, flats30, flats15)
    points (x,y,z) triangles materials
general slopes (flats, gradual, hills, mountain)
water flow, amount of water
    random direction per tile
    breaking circles from the coast with flipping
tree/plant growth
steep sides into rock faces
lakes as non-connected seas
roofs / walls / roads / rails (flatten)
editing height and open terrain
    show pre-build items & place them with position and rotation
    clicking including height and multiple layers
shortest path
    on single layer
    connections between layers
    width of room versus vehicle
    maximum slope (different materials)
    maximum water depth to pass (wading, animals, vehicle)
    following water for boats (ocean, sea, river, lake)
