[package]
name    = "probe_r0"
version = "0.0.1"
loft    = ">=0.8.5"

# ⚠ `imaging` is NOT a dryopea dependency and must not become one on the
# strength of this probe.  It is here because the probe's second half asks
# whether a CAPTURED FRAME can be decoded and classified — see
# docs/RENDERER.md § R0.  If plan 21 R4 makes the GL gate real, `imaging`
# joins dryopea's own loft.toml then, with that phase as the reason.
[dependencies]
graphics = ">=0.5.0"
imaging  = ">=0.2.0"
