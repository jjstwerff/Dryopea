fn _tp_integer_abs(both:integer) -> integer {
  OpAbsInt(both);
}#integer

fn _tp_long_abs(both:long) -> long {
  OpAbsLong(both);
}#long

fn _tp_single_abs(both:single) -> single {
  OpAbsSingle(both);
}#single

fn _tp_single_cos(both:single) -> single {
  OpMathCosSingle(both);
}#single

fn _tp_single_sin(both:single) -> single {
  OpMathSinSingle(both);
}#single

fn _tp_single_tan(both:single) -> single {
  OpMathTanSingle(both);
}#single

fn _tp_single_acos(both:single) -> single {
  OpMathAcosSingle(both);
}#single

fn _tp_single_asin(both:single) -> single {
  OpMathAsinSingle(both);
}#single

fn _tp_single_atan(both:single) -> single {
  OpMathAtanSingle(both);
}#single

fn _tp_single_atan2(both:single, v2:single) -> single {
  OpMathAtan2Single(both, v2);
}#single

fn _tp_single_ceil(both:single) -> single {
  OpMathCeilSingle(both);
}#single

fn _tp_single_floor(both:single) -> single {
  OpMathFloorSingle(both);
}#single

fn _tp_single_round(both:single) -> single {
  OpMathRoundSingle(both);
}#single

fn _tp_single_sqrt(both:single) -> single {
  OpMathSqrtSingle(both);
}#single

fn _tp_single_log(both:single, v2:single) -> single {
  OpMathLogSingle(both, v2);
}#single

fn _tp_single_pow(both:single, v2:single) -> single {
  OpPowSingle(both, v2);
}#single

fn _tp_float_abs(both:float) -> float {
  OpAbsFloat(both);
}#float

fn PI() -> float OpMathPiFloat()

fn E() -> float OpMathEFloat()

fn _tp_float_cos(both:float) -> float {
  OpMathCosFloat(both);
}#float

fn _tp_float_sin(both:float) -> float {
  OpMathSinFloat(both);
}#float

fn _tp_float_tan(both:float) -> float {
  OpMathTanFloat(both);
}#float

fn _tp_float_acos(both:float) -> float {
  OpMathAcosFloat(both);
}#float

fn _tp_float_asin(both:float) -> float {
  OpMathAsinFloat(both);
}#float

fn _tp_float_atan(both:float) -> float {
  OpMathAtanFloat(both);
}#float

fn _tp_float_atan2(both:float, v2:float) -> float {
  OpMathAtan2Float(both, v2);
}#float

fn _tp_float_ceil(both:float) -> float {
  OpMathCeilFloat(both);
}#float

fn _tp_float_floor(both:float) -> float {
  OpMathFloorFloat(both);
}#float

fn _tp_float_round(both:float) -> float {
  OpMathRoundFloat(both);
}#float

fn _tp_float_sqrt(both:float) -> float {
  OpMathSqrtFloat(both);
}#float

fn _tp_float_log(both:float, v2:float) -> float {
  OpMathLogFloat(both, v2);
}#float

fn _tp_float_pow(both:float, v2:float) -> float {
  OpPowFloat(both, v2);
}#float

fn _tp_text_len(both:text) -> integer {
  OpLengthText(both);
}#integer

fn _tp_vector_len(both:vector) -> integer {
  OpLengthVector(both);
}#integer

fn print(v1:text) {
  OpPrint(v1);
}

