fn _tp_integer_abs(both:integer) -> integer {#block:integer
  OpAbsInt(both);
}#block:integer

fn _tp_long_abs(both:long) -> long {#block:long
  OpAbsLong(both);
}#block:long

fn _tp_single_abs(both:single) -> single {#block:single
  OpAbsSingle(both);
}#block:single

fn _tp_single_cos(both:single) -> single {#block:single
  OpMathCosSingle(both);
}#block:single

fn _tp_single_sin(both:single) -> single {#block:single
  OpMathSinSingle(both);
}#block:single

fn _tp_single_tan(both:single) -> single {#block:single
  OpMathTanSingle(both);
}#block:single

fn _tp_single_acos(both:single) -> single {#block:single
  OpMathAcosSingle(both);
}#block:single

fn _tp_single_asin(both:single) -> single {#block:single
  OpMathAsinSingle(both);
}#block:single

fn _tp_single_atan(both:single) -> single {#block:single
  OpMathAtanSingle(both);
}#block:single

fn _tp_single_atan2(both:single, v2:single) -> single {#block:single
  OpMathAtan2Single(both, v2);
}#block:single

fn _tp_single_ceil(both:single) -> single {#block:single
  OpMathCeilSingle(both);
}#block:single

fn _tp_single_floor(both:single) -> single {#block:single
  OpMathFloorSingle(both);
}#block:single

fn _tp_single_round(both:single) -> single {#block:single
  OpMathRoundSingle(both);
}#block:single

fn _tp_single_sqrt(both:single) -> single {#block:single
  OpMathSqrtSingle(both);
}#block:single

fn _tp_single_log(both:single, v2:single) -> single {#block:single
  OpMathLogSingle(both, v2);
}#block:single

fn _tp_single_pow(both:single, v2:single) -> single {#block:single
  OpPowSingle(both, v2);
}#block:single

fn _tp_float_abs(both:float) -> float {#block:float
  OpAbsFloat(both);
}#block:float

fn PI() -> float OpMathPiFloat()

fn E() -> float OpMathEFloat()

fn _tp_float_cos(both:float) -> float {#block:float
  OpMathCosFloat(both);
}#block:float

fn _tp_float_sin(both:float) -> float {#block:float
  OpMathSinFloat(both);
}#block:float

fn _tp_float_tan(both:float) -> float {#block:float
  OpMathTanFloat(both);
}#block:float

fn _tp_float_acos(both:float) -> float {#block:float
  OpMathAcosFloat(both);
}#block:float

fn _tp_float_asin(both:float) -> float {#block:float
  OpMathAsinFloat(both);
}#block:float

fn _tp_float_atan(both:float) -> float {#block:float
  OpMathAtanFloat(both);
}#block:float

fn _tp_float_atan2(both:float, v2:float) -> float {#block:float
  OpMathAtan2Float(both, v2);
}#block:float

fn _tp_float_ceil(both:float) -> float {#block:float
  OpMathCeilFloat(both);
}#block:float

fn _tp_float_floor(both:float) -> float {#block:float
  OpMathFloorFloat(both);
}#block:float

fn _tp_float_round(both:float) -> float {#block:float
  OpMathRoundFloat(both);
}#block:float

fn _tp_float_sqrt(both:float) -> float {#block:float
  OpMathSqrtFloat(both);
}#block:float

fn _tp_float_log(both:float, v2:float) -> float {#block:float
  OpMathLogFloat(both, v2);
}#block:float

fn _tp_float_pow(both:float, v2:float) -> float {#block:float
  OpPowFloat(both, v2);
}#block:float

fn _tp_text_len(both:text) -> integer {#block:integer
  OpLengthText(both);
}#block:integer

fn _tp_vector_len(both:vector) -> integer {#block:integer
  OpLengthVector(both);
}#block:integer

fn print(v1:text) {#block:void
  OpPrint(v1);
}#block:void

