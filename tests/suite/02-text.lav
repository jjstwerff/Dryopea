fn main() {
    a = "1" + "2";
    assert("'{a:4}'" == "'12  '", "Formatting text");
    assert(len(a + "123") == 5, "Text length");
}

// for loop over utf-8 characters
// index to get a character
// range to get a sub-string
// append characters to a result
// library functions: starts_with
