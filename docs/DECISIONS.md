<!--
Copyright (c) 2026 Jurjen Stellingwerff
SPDX-License-Identifier: LGPL-3.0-or-later
-->

# DECISIONS — the greppable index of what was decided and what was measured

⚠ **This file is an INDEX, never a source.**  Every row points at the document
that owns the decision; the row is one line and the reasoning stays where it
was written.  A second copy of a decision is the copy that drifts.

## The code scheme

Four namespaces, one letter each, three digits, **never reused** — the same
convention `PROBLEMS.md` already uses for `@D001`.

| code | is | index | grep |
|---|---|---|---|
| `@X###` | a **design decision** | this file § Decisions | `grep -rn '@X014' docs/ plans/` |
| `@M###` | a **measurement of record** — a number other documents quote | this file § Measurements | `grep -rn '@M001' .` |
| `@D###` | a **dryopea defect** | [`PROBLEMS.md`](../PROBLEMS.md) | `grep -rn '@D002' .` |
| `@P###` | a **loft** issue (upstream) | loft's own tracker | — |

### ⚠⚠ Plan phases are NOT unique, and must be written `<plan>-<phase>`

A bare phase code is ambiguous today and grep cannot resolve it:

| bare code | plans that use it |
|---|---|
| `S0` | **18** (scenario capture) and **22** (the field cache) |
| `C2` | **09** (lattice conversion) and **15** (the carry model) |
| `V0` | **08**, **13** and **19** |
| `R0` | **20** and **21** |

⚠ So cite a phase as **`19-P3`**, **`22-S0`**, **`12-B7`** — plan number, hyphen,
phase.  That form is unique, greppable, and reads the same as the prose already
does (*"plan 12 B7"*).  ⚠ Existing prose that says "plan 12 § B7" is fine and is
not being rewritten; what matters is that a **code written on its own** carries
its plan.

### Categories

Every `@X` carries one, so the index can be filtered:

| category | what it governs |
|---|---|
| `world` | the lattice, terrain, heights, passability |
| `sim` | the tick, movement, damage, waves, cost |
| `render` | cameras, geometry, frames, art |
| `play` | the loop, input, what the player does |
| `run` | the shape of a session and a run |
| `method` | how the project builds and gates things |

---

## Decisions

### render — entity art ([`PARTS.md`](PARTS.md))

| code | decision | owner |
|---|---|---|
| `@X001` | ⚠ **REVISED 2026-08-15** — ~~a dryopea-native part model~~ → **reuse `hex_body`** (published: rigs, revolute joints with limits, pure poses, per-bone OBBs, canonical text) and **enhance it** with an out-of-plan hinge axis.  ⚠ The first draft read `hex_part` (unpublished) and never checked the registry.  ⚠ `hex_part` itself stays out — trigger unchanged | `PARTS.md` § D1 |
| `@X002` | ⚠ **REVISED 2026-08-15** — ~~box/disc primitives~~ → **hex CELLS WITH HEIGHTS**, authored at the part's own scale.  ⚠ The roundness objection dissolves on `@X044`'s corner smoothing; boxes would have forfeited six published libraries | `PARTS.md` § D2 |
| `@X003` | **the tower's socket is the SIMULATION's** — plan `17-T2`'s detachable top already is moros's socket model, so what is drawn asks `TowerState` and never a second flag | `PARTS.md` § D3 |
| `@X004` | a part emits **GEOMETRY**; there is no sprite and no baked scale.  ⚠ Replaced an earlier sprite design — a sprite sheet does not degrade under a free camera, it *lies* | `PARTS.md` § D4 |
| `@X005` | a **PNG is an ARTEFACT**, never a runtime input.  ⚠ `imaging::Pixel` has no alpha, but a captured frame is opaque, so the hole does not bite | `PARTS.md` § D5 |
| `@X006` | **the durable artefact is the SIZE**, and a test compares a part's footprint to the simulation's constant | `PARTS.md` § D6 |
| `@X007` | the **hover unit**: fixed base, four booms, front rotors r 0.36, **rear r 0.52**, canopy on a rear lateral hinge `0 → 0.30` turns | `PARTS.md` § D7 |
| `@X044`-`@X047` | the terrain mesh's construction, copied from moros — corner heights are a MEAN; **one predicate** (`faced_between`) both stops the blend and draws the wall; emit a face only from the side that stands; the mesh tile is not the store tile.  ⚠ **`@X044` and `@X045` are SUPERSEDED for dryopea by `@X072`** — the mean is a no-op here and there is no blend to stop.  ⚠ **`@X046` is BUILT** (plan 25 M1, `ground_mesh.loft::ground_side_faces`), and both halves of its `hh <= nh` fail invisibly, so it is gated as four COUNTS rather than as a picture | [`RENDERER.md`](RENDERER.md) § R3, `plans/25` § M1 |
| `@X048` | ⚠⚠ **a cell is a COLUMN OF LAYERS**, each with an absolute height — that is where vertical detail comes from.  ⚠ Parts get columns now; the TERRAIN keeps one surface until something must walk UNDER something | `PARTS.md` § D2 |
| `@X051` | **BRIDGES span HOLES, not walkable ground** — `DESIGN.md` makes a cavern a *"hole in the surface map, a non-walkable kind the way sea already is"*, so a span is just a hex at height and ONE surface still answers.  ⚠ The layer trigger fires only if a bridge must cross walkable ground; that is a MOVEMENT change (`Hex` → `(Hex, layer)`), not an art one.  ⚠⚠ A span *"cannot be walled and cannot be flanked"* — terrain that invalidates the learned optimum `@M004` teaches | `PARTS.md` § D2 |
| `@X052` | ⚠⚠ **base bridges robots walk UNDER fire `@X048`'s trigger** — two walkable surfaces over one hex, so the flow field's node becomes `(Hex, layer)` and `hex_height` / `can_step` / occupancy / 1 094 tests are all in scope.  ⚠ **Undesigned, and flagged as such by the project owner.**  `DESIGN.md` already names the feature (`cy`-layer decks, second-phase).  ⚠ Three seeds: a tower above its own body ramp, a crew route the wave does not share, somewhere to park that is not in the way | `PARTS.md` § D2 |
| `@X049` | a **PLACEMENT layer** — where another form MAY go (`SOCK`/`FITS`, `hex_fit`'s doorstep).  ⚠ A refusal must NAME its restriction, because an off-grid value is otherwise **silently snapped**.  ⚠ dryopea has one already: the tower socket | `PARTS.md` § D2 |
| `@X050` | **attachment with LIMITS** — `hex_body`'s `Joint {value, lo, hi}` plus `joint_fits` / `joint_offer` (clamped, not refused) / `joint_residual` (how far past), and `rig_admissible` for a whole pose | `PARTS.md` § D2 |
| `@X038`-`@X043` | the build pipeline — a script in, a `.glb` and validation frames out.  ⚠ `mesh3d` + `glb` are published, so the emitter is a MAPPING; the glb is a baked POSE not a rig; validation is measured **and** a cold read | `PARTS.md` § D9 |
| `@X008` | the **crew is the same part** as the player (*"same chassis"*), and the four robot classes are **one part with per-class DATA** | `PARTS.md` § D8 |
| `@X087` | ⚠⚠ **a socket rides a BONE at `t`, a binding carries NEITHER a coordinate NOR a pose, and where a socket IS, is COMPUTED.**  Plan 20 A1's model, `src/part.loft` over `hex_body::Rig`.  ⚠ **The absence is the mechanism** (`hex_part::bind.loft`'s own invariant): move the tower and its top follows, because there is no second coordinate that could be forgotten — so `socket_world` is the ONE re-assertion site and a `bd_q` added later *for convenience* would end the design without failing a test.  ⚠ **Three deviations from `hex_part`'s records, each forced rather than chosen**: a socket is at a bone and a `t` along it because a part here is a `Rig` and not a voxel world (and `t` is the family's own spelling — `hex_fit::feature_fits`); **one class token** rather than a kind AND a size, because dryopea's size is gated against the SIMULATION's constant and a size here would be a second authority on one fact (`@X006`); and **no `bd_open`**, because that field is right in a saved DOCUMENT and wrong in a catalogue asset whose angles come from the sim (`@X003`) — storing one would be exactly the second flag § D3 refuses.  ⚠ **The refusals are dryopea's `*_fault` text rather than `hex_part`'s code-plus-why**, and what survives the change of spelling is `K-FIT`'s real requirement: a refusal NAMES its restriction, so a missing part and a cycle never share a wording.  ⚠⚠ **The cycle check carries a PATH, not a visited set**, and the DIAMOND is what tells them apart — one part in two sockets is legal, and a global *seen* set refuses a catalogue that is fine (`@M036`) | `plans/20` § A1, `part.loft`, `PARTS.md` § D3, `@X001`, `@X003`, `@X006`, `@M036` |
| `@X088` | ⚠⚠ **A PART'S FOOTPRINT IS DERIVED FROM ITS LIMBS, NEVER DECLARED BESIDE THEM — and the vehicle is 2.28 x 2.05 because that is where the ROTORS are.**  Plan 20 A3.  `PARTS.md` § D6 asks that *"a part declares its footprint, and a test compares it to the number the simulation uses"*, and a part that DECLARED its size would agree with itself for ever — so `src/catalogue.loft` declares LIMBS and `part_size` computes the extent.  ⚠⚠ **The size question was the project owner's and it was a SIMULATION change** (2026-08-18): § D7's 2.28 x 2.05 wins over `numbers.json`'s 1.1 x 2.4, which is a different SHAPE rather than a bigger one — a wide short quadcopter against a long narrow car — and the vehicle is now wider than the 1.5 m hex it occupies, with `blocker_at` unchanged and nothing deriving passability from art.  ⚠ The HEIGHT is **0.93** and not § D7's rounded *"~0.9"*, because it is derived too: skid bottom −0.37 to canopy top +0.56.  ⚠ **Three copies and two gates** — the limb table, the `.loft` constant, `numbers.json` — because nothing loads the JSON.  ⚠ Three things the catalogue deliberately does NOT carry: a colour (a class is a row of DATA, so one would force an entry per class, § D8), a limb SOLIDITY (dryopea's hitbox is a HEX, and reading one would be the *derive passability from art* § D6 forbids), and § D7's four BOOMS (they run diagonally and nothing carries a REST ORIENTATION — A2's to solve) | `plans/20` § A3, `catalogue.loft`, `PARTS.md` § D6 / § D7, `@X006`, `@X007`, `@M037` |
| `@X089` | ⚠⚠ **A PART EMITS TRIANGLES AND CONTAINS NO FORWARD KINEMATICS — the pose comes from the LIBRARY, one frame per BONE.**  Plan 20 A2, `src/part_mesh.loft`.  Every vertex is placed by `hex_body::frame_point` over a `rig_world_frame3` held across that bone's limbs; a second implementation of a pose is the one a future reader would reach for and would drift from the one the sockets use (`@X001`).  ⚠⚠ **`rig_world_frame3` EXISTS BECAUSE OF THIS PHASE**: `rig_world_seg3` answers a bone's base and tip, both ON its axis, and a limb is a cloud of points that are not — so the basis it computed and discarded became `hex_body` **0.3.0**, proposed and published upstream rather than re-derived here.  ⚠ A NORMAL is a DIRECTION: posing one as a point adds the bone's translation, which still normalises to something plausible and is wrong everywhere the bone is not at the origin — gated by a limb 13 m out.  ⚠ A box gives each face its OWN four corners (24 vertices, not 8) so the normals stay flat, which is `ground_mesh.loft`'s no-blend argument one layer up.  ⚠ **No colour** (a class is a row of DATA and colour is a UNIFORM, `@X074`) and **no world placement** (that is A5, and the only layer that knows `play_alpha`) | `plans/20` § A2, `part_mesh.loft`, `@X001`, `@X004`, `@M038` |
| `@X090` | ⚠⚠ **WHAT IS DRAWN IS READ FROM THE SIMULATION, AND THE CANOPY READS THE CARGO LEDGER.**  Plan 20 A4, `src/pose.loft`.  § D3's rule — *the sprite follows the simulation, and never a second flag* — applied to all three joints the game has: the tower's top is `tower_has_top` (plan 17 T2), the rotors' rate is `vehicle_speed` (plan 13), and the canopy is **`cargo_carrying`** — open exactly while this hover unit is laden.  ⚠ The canopy is the decision, because nothing forced it: it is the one thing `carry.loft` moves that nothing draws, so an open cockpit makes a laden vehicle legible at a glance, which is `DESIGN.md` § HUD's *diegetic* rule getting something for free.  ⚠ A field on `Vehicle` saying *canopy open* would be the defect the whole design is arranged to make unwritable — two facts that can disagree about one thing.  ⚠⚠ **The simulation answers a TARGET and the SWING is presentation**: `pose_hover_unit` takes the angle as a FLOAT so the renderer can ease it (`fixstep::approach`, the camera's own `k`), because a binary door here would have re-introduced the two-pose canopy § D4 retired one layer down | `plans/20` § A4, `pose.loft`, `PARTS.md` § D3, `@X003`, `@X004`, `@M039` |
| `@X091` | ⚠⚠ **A ROTOR'S BOOST RATE IS DERIVED FROM THE VEHICLE'S OWN SPEED RATIO, AND ITS IDLE RATE IS BOUNDED BY THE FRAME RATE RATHER THAN BY THE FICTION.**  Plan 20 A4.  `rotor_turns_per_second` is `ROTOR_IDLE_TURNS_PER_S x (vehicle_speed(v) / VEHICLE_SPEED_HEX_PER_SECOND)`, so the rotors speed up by exactly as much as the vehicle does and there is no second number that can disagree about what boost IS.  ⚠⚠ Two crossed blades repeat every quarter turn, so the apparent direction REVERSES beyond an eighth of a turn per frame — **3.75 turns/s at 30 fps** — while a believable rotor turns at fifty.  So 1.2 is stylised, deliberately, with the boosted figure at 2.4 and a 1.5x margin that a test pins.  ⚠ The PHASE is `rate x t` read fresh rather than integrated, so a change of rate moves it; integrating would need an accumulator on the `Vehicle`, which is the animation state this plan refuses, and the trigger to add one is *something READS a blade's position* | `plans/20` § A4, `pose.loft`, `PARTS.md` § D7, `@X007`, `@M039` |
| `@X092` | ⚠⚠ **AN ENTITY COLOUR IS DELIBERATELY OUTSIDE THE PALETTE, AND THE GATE ACCOUNTS FOR EVERY NON-PALETTE PIXEL BY NAME.**  Plan 20 A5, `src/entity_view.loft`.  `measure.loft`'s founding rule is that a pixel is an EXACT palette colour or a FAULT; an entity painted in a palette colour would be counted as GROUND, so a robot on grass would be absorbed into a band tens of thousands of pixels wide and every share would still read plausible.  So an entity colour is none of the twelve, it lands in `classify_canvas`'s `unknown` exactly as the background does, and `gl_gate.loft::gl_entity_pixels` sums the ten by name.  ⚠ The claim that buys is TOTAL and exact: **every pixel is a palette colour, an entity colour, or the clear colour, and nothing else** — asserted as `unknown - entity pixels == 0` over a frame-filling world with a roster standing in it.  ⚠ `entity_colours_distinct` is what stops it being a wish, and it fired at once (`@M040`).  ⚠ The alternative — a thirteenth bucket for entities — was refused for `@X077`'s reason one layer on: fold them into the classifier and the ONE thing `unknown` is for stops meaning *fault* | `plans/20` § A5, `entity_view.loft`, `gl_gate.loft`, `@X074`, `@X077`, `@M040` |
| `@X093` | ⚠⚠ **A DRAWN CLASS *IS* THE SIMULATION'S MOVER KIND — THERE IS NO SECOND ENUMERATION.**  Plan 20 A5.  `PARTS.md` § D8 makes a class *a row of DATA (a colour, a scale factor, at most one distinguishing limb)*, which is `CLAUDE.md`'s *ONE AI, per-class DATA* one layer up.  So `entity_colour` is indexed by `passable.loft`'s own discriminants, unmapped — `KIND_VEHICLE` is the player's row at 2, scar and all — and the three rows that are not movers (crew, tower base, tower top) APPEND after the last one, which is that file's own rule.  ⚠ **The consequence is that a new robot class costs a colour and nothing else**: no art-side enumeration to keep in step, and therefore no pair that can disagree about which class a robot is.  ⚠ `entity_class_name` delegates to `enemy_class_name` for every mover row for the same reason | `plans/20` § A5, `entity_view.loft`, `passable.loft`, `PARTS.md` § D8, `@X008` |
| `@X094` | ⚠⚠ **A HOVER UNIT IS DRAWN AT THE HEIGHT IT CAN CLIMB, SO BOOST IS VISIBLE FOR NOTHING.**  Plan 20 A5.  `PROXY_ART.md` gives the vehicle *~0.4 m hover clearance* and `vehicle.loft` gives it `VEHICLE_CLIMB_METRES = 0.4` — the same number twice, because the reason a hover unit crosses a 0.4 m step is that it floats 0.4 m off the ground.  So the renderer reads **`vehicle_climb(v)`** rather than restating the constant: there is no second number to disagree, and a boosting vehicle (climb 3.0 m) is DRAWN 3.0 m up, clearing the wall it is about to clear.  ⚠ That is `DESIGN.md` § HUD's *diegetic* rule getting a second thing for free, after the rotors (`@X091`) — and unlike the rotors it survives a flat unlit colour, because it moves the silhouette rather than turning it (`@M039`).  ⚠ A crew member has no boost door, so it floats the idle figure; `entity_emit_hover` decides on the OWNER id, which is the same integer `CargoLayer` is keyed on | `plans/20` § A5, `entity_view.loft`, `vehicle.loft`, `PROXY_ART.md`, `@X091`, `@M039` |
| `@X095` | ⚠⚠ **THE RENDERER DERIVES ITS OWN INVALIDATION BY DIFFING THE HEIGHT LAYER — THE SIMULATION IS NEVER TOLD ANYBODY IS WATCHING.**  Plan 19 P6, `src/play_view.loft`.  `ground_gl.loft` was written for an EDITOR's ground (paint a hex, re-bake the tiles it reaches); a live session moves the terrain for three other reasons — a body falls, a wall breaks, a heap is cleared — and none of them goes near `paint` from the caller's side.  ⚠ A dirty list on `HeightLayer` was the obvious answer and is refused on three counts: it would leak into `state_diff` (which asks *are two runs in the same state*, and a dirty list is not state), survive an `emit`, and grow for ever in the 1397 headless tests that have no renderer.  ⚠⚠ **So `MeshWatch` keeps a SNAPSHOT and diffs it, which is exact only because of one invariant: *every terrain change a TICK can make moves the HEIGHT LAYER*** — a body raises its hex, a breach raises masonry BEFORE it repaints (`break_structure`'s own order), salvage lowers a pile.  The layer is a handful of entries even on a busy base, so the diff is O(piles).  ⚠ **It is asserted against the simulation and not quoted**: `tests/19_p6` plays a real base and compares every maintained tile with a COLD rebuild, which is `11_f8`'s field-cache shape — and the fixture's own check found the first version measuring a base where only BODIES fell, so the one case the diff cannot see directly (a REPAINT) had never happened. | `plans/19` § P6, `play_view.loft`, `mesh_chunks.loft`, `@X075`, `@M041` |
| `@X096` | ⚠⚠ **THE GROUND TILE IS 8x8, DECIDED ON THE MEASUREMENT PLAN 25 M4 COULD NOT MAKE.**  M4 swept `MESH_CHUNK_SHIFT` and **changed nothing**, deliberately: shrinking the tile buys 8.6x on an edit and costs 12x the draw calls, *"and that half cannot be measured from `loft test`"*.  It named its inheritor — *the phase that wires GL into `play_mode`* — and plan 19 P6 is it.  ⚠⚠ **Measured in a real window: the draw cost does not move at all.**  96 tiles draw in the same 7 ms/frame as 8, and 24 in the same 5 ms as 4 — so the tile size is decided by the EDIT cost alone, where 8x8 is **7 ms against 54 ms** on a small world and **7 ms against 112 ms** on one four times the area.  ⚠ At 32x32 a body falling cost a 3-7 frame hitch that GREW with the map; at 8x8 it is constant at 7 ms, inside a 60 fps frame.  ⚠ The cold bake on entering play mode got FASTER too (218 ms against 259), which is M4's own finding that a big tile at the world's edge walks more hexes that are not drawn.  ⚠⚠ **The cost was two test fixtures that encoded 32**, and both are now derived from `mesh_chunk_span()` and pass at 8, 16 and 32: `25_m2`'s coordinates are a TILE BOUNDARY rather than the number 32, and `25_m4`'s two thresholds (a literal `> 500` hexes' worth, and a `> 10x` sparse/dense ratio) were each a statement about the world they were measured in rather than about the mesher. | `plans/19` § P6, `plans/25` § M4, `mesh_chunks.loft`, `@M028`, `@M041` |
| `@X097` | ⚠⚠ **THE HUD IS ONE NUMBER, AND ITS DIGITS ARE RECTANGLES.**  Plan 19 P7.  Two decisions, and the first is `docs/DESIGN.md` § HUD's rather than this phase's: its entire numeric HUD is *"Wallet (points) — one corner number.  The only number the player must see to make build decisions. […] That's the entire HUD.  No wave-number, no inter-wave countdown, no minimap, no boost cooldown bar."*  So there is no health bar, no tick counter, no enemy count and no wave readout, and **adding one is a design change rather than a feature** — everything else is diegetic and already built (the rotors show boost `@X091`, the canopy shows cargo `@X090`, a tower's top shows whether it has one).  ⚠⚠ **The second is forced**: `graphics` publishes `draw_text`, and it is unusable twice over — it rasterises through `#native rasterize_text_into`, which answers *"native function not loaded"* under `loft test`, and it needs a FONT FILE this repo does not have.  **A HUD nothing headless can draw is a HUD no test and no `snap` can see**, so the digits are SEVEN SEGMENTS built from `fill_rect`, which every gate in the repo already reads.  ⚠ `picker.loft` reached the same conclusion in plan 01 and its note is the older half of this one.  ⚠ Right-aligned, because a corner readout that grows leftward never moves the digit the eye is resting on; FLOORED, because a tower costs 100 whole points and rounding 99.6 up would show one the player cannot buy; and 0 for a broke run rather than a minus sign. | `plans/19` § P7, `hud.loft`, `play_view.loft`, `DESIGN.md` § HUD, `@X090`, `@X091`, `@M042` |
| `@X098` | ⚠⚠ **THE WALLET'S NUMBER RAMPS AMBER TO RED, AND IT IS A FUNCTION OF THE INTEGER THE DIGITS SHOW.**  Plan 19 P7, `hud.loft` § the ramp (project owner, 2026-08-18 — `@X097` had left it as an open design question and the answer was *add it*).  ⚠ **LINEAR over `WALLET_STARTING_POINTS`**, which is the one curve that invents no number: the two endpoint colours are the only additions, and the span is a constant the simulation already owns.  ⚠⚠ **The threshold a designer would reach for is refused for now** — *the number goes warm when you can no longer buy the cheapest thing* needs `order_cost_points` (100), which lives in `examples/numbers.json` and in **no `.loft` constant**, because building is not built.  Introducing a simulation constant nobody spends, to colour a HUD, is a constant that drifts; the trigger to add the band is BUILDING.  ⚠⚠ **It reads `hud_wallet_points` and not `wallet_left`, and that is two decisions in one.**  The colour can never disagree with the number, because it IS the number — read the float instead and they are two readings of one wallet that can differ by a rounding, invisibly, both plausible.  And it makes the reachable colours FINITE: exactly 201, so `tests/19_p7` sweeps every one of them against the palette, the entity table and the clear colour rather than sampling.  ⚠ That sweep is what caught `@M043`. | `plans/19` § P7, `hud.loft`, `DESIGN.md` § HUD, `@X097`, `@X092`, `@M043` |

### render — the camera and the pipeline ([`RENDERER.md`](RENDERER.md))

| code | decision | owner |
|---|---|---|
| `@X009` | the camera is **moros's `RenderCamera`**, ported not depended on.  ⚠⚠ `camera_overview` at elevation **89° IS dryopea's editor view**, so it is one camera with two presets | `RENDERER.md` § R1 |
| `@X010` | **geometry is shared, rasterisation is not** — GL for the game, the software rasteriser keeps the editor and its 520 measurements.  ⚠ Trigger to collapse: GL reproduces the top-down view | `RENDERER.md` § R2 |
| `@X011` | the **terrain becomes 3-D**, meshed per dirty chunk.  ⚠ The largest single item; probably its own plan | `RENDERER.md` § R3 |
| `@X012` | the gate chain is **xvfb → GL → `gl_screenshot` → `imaging::png` → exact classification** | `RENDERER.md` § R4, `@M002` |
| `@X013` | the gate renders **FLAT UNLIT** — shading turns one palette colour into a range and `unknown` stops meaning *fault*.  ⚠ Never loosen to nearest-colour | `RENDERER.md` § R4 |
| `@X014` | the game's camera lives on **`PlayState`**, never `EditorState.cam`.  ⚠ **BUILT in plan 21 R2**, one phase after the camera itself — until the ease existed there was nothing for a session to REMEMBER between frames | `RENDERER.md` § Open 4, `@X070` |
| `@X015` | **no indoor camera modes** — moros's SNUG / CUTAWAY / EYES answer an interior dryopea's roofless bases do not have | `RENDERER.md` § R1 |
| `@X065` | ⚠⚠ **the 3-D world frame is RIGHT-HANDED — x EAST, y NORTH, z UP — and `lat_to_world` is the ONE place that negates.**  dryopea's `+y` is SOUTH everywhere else, which is a CANVAS convention; carried into 3-D it is left-handed, `mat4_look_at` builds a right-handed basis, and the product is a MIRROR.  ⚠ The negation cancels `lat_to_metres`', so the camera's world is `hex_grid`'s own frame — the library's is a WORLD frame and dryopea's is a SCREEN frame | `RENDERER.md` § R1, `@M021` |
| `@X066` | **`camera_overview` pins the AZIMUTH as well as the elevation** (270°) — a deviation from moros, which leaves it free because a map view there rotates.  dryopea's editor view does not rotate, so an unpinned overview would reproduce it from one orbit position and be a rotated picture of it from every other | `RENDERER.md` § R1, `@M021` |
| `@X067` | **a follow bearing is `azimuth = facing + 180°`, and it comes from the VELOCITY** — the vehicle stores no facing.  ⚠ moros's `azimuth = 270° − facing_deg` says the same thing in moros's frame and puts the camera **ABEAM** in dryopea's (`@M022`).  ⚠ A parked vehicle has NO bearing, so the camera keeps the one it had — `atan2(0, 0)` is 0.0 and would swing the view east on every key release | `RENDERER.md` § R1 + § Open 1, `@M022` |
| `@X068` | ⚠⚠ **the ease is `1 − e^(−k·dt)`, and moros's `f = k·dt` is REFUSED** — the linear form is frame-rate DEPENDENT, and `play.loft` is built on the opposite property (`19-P0`: *1200 frames of 1/60 s and one frame of 20 s reach the same state*).  A camera that disagreed would make a captured frame a function of the frame rate.  ⚠ It also fixes the camera's step to the FRAME clock — stepped last in `play_step`, never inside `play_advance`'s tick loop, where at 60 fps it would run on one frame in forty | `RENDERER.md` § R2b, `@M023` |
| `@X069` | ⚠⚠ **the ease carries THREE valves — target, azimuth and boom — where moros eases the boom alone**, and the reason is dryopea's own: the vehicle is on a LATTICE, so its position is two integers and moves a whole hex (1.299 m) on the tick it steps.  An un-eased follow camera is not a camera with a rough edge, it is a slide show.  ⚠ `plans/21` § R2 names only the boom; that is the phase under-describing itself | `RENDERER.md` § R2b, `@M023` |
| `@X070` | **the rig holds TWO booms** — `boom` is what the player asked for (the wheel writes it), `cam.distance` is what the eye HAS after easing and occlusion.  ⚠ Collapse them and a wall the vehicle drove past permanently rewrites the zoom.  ⚠ Rest also SNAPS: an exponential approach is asymptotic, so stopping at a tolerance leaves the resting eye a function of how the frames fell — moros measured a 0.025-unit spread from exactly that | `RENDERER.md` § R2b, `@M024` |
| `@X071` | **ONE sight walker, in `passable.loft`, and it answers WHERE rather than whether** — `sight_first_block` returns the blocking cell's index, `tower_sees` is the boolean door onto it and `camera_boom_free` the distance door.  ⚠ It walks BOTH endpoints, which `tower_sees` used to skip: a looker's eye sits on top of its own hex, so the skip was never load-bearing — and the camera's far endpoint is the EYE, which is exactly the case that must be checkable | `RENDERER.md` § R2b, `ARCHITECTURE.md` |
| `@X072` | ⚠⚠ **the terrain mesh does NOT blend — flat tops and vertical sides, no corner-height mean.**  `@X044` (moros's mean of the hex and the neighbours sharing a corner) is what makes terrain slope instead of step, and in dryopea it is a **no-op at every hex in both directions**: `height_override` is non-null on exactly two of twelve palette kinds, so across ground every term of the mean is 0, and across a structure's edge `@X045`'s `faced_between` skips it anyway.  ⚠ **And it is honest rather than cheap**: the simulation asks `can_step`, a height DIFFERENCE, so a mesh that sloped where the rule steps would draw a ramp the vehicle cannot climb — flat tops make a height difference readable as the SIZE OF A QUAD, which is what the player is deciding about.  ⚠ The trigger to revisit is [`plan 02`](../plans/02-solver-validation-viewer/README.md)'s slope solver, and `plans/25` M2's halo gate is the tripwire | `plans/25` § What was measured first |
| `@X073` | **the corner↔direction relation is `hex_grid::hex_edge_corners`, delegated and never tabulated** — a side quad needs the two corners bounding the edge towards neighbour `dir`, the library already answers it, and a hand-written table would be a second lattice.  ⚠ The same discipline as `lat_neighbour` being the only stepper, and moros#10 is what a second lattice costs | `plans/25` § What was measured first, `lattice.loft` |
| `@X075` | ⚠⚠ **the drawn region is the painted set PLUS A ONE-HEX RING, and the mesher has its own tiling with no `ChunkField` in it.**  `painted.loft` ERASES a hex painted `sea`, so "mesh the painted set" leaves an erased region as a HOLE in the ground at exactly the height of the land around it — and no side quad covers it, because sea is 0 m and so is grass.  ⚠ The two rejected candidates each fail on a principle: the painted set's **bounding box** draws a quarter-million hexes of ocean between two distant bases, and the **tile's full extent** makes how far the ocean reaches a function of where the tile boundaries fell — which is `plans/22`'s *granularity must not follow the implementation* in a new place.  ⚠ **Its limit is asserted, not assumed**: an erased gap wider than two hexes still holes, and a test pins it so closing it is deliberate.  ⚠ The real answer is water's **DROP**, which the palette already carries per kind and nothing reads — a SIMULATION decision, so it belongs with [`plan 02`](../plans/02-solver-validation-viewer/README.md).  ⚠⚠ And **two reaches share the value 1 without sharing a constant**: `MESH_HALO_K` (how far an edit's consequences reach) and the drawn ring (how far past the paint anything is drawn) move independently the day a blend lands.  ⚠ No `ChunkField`: `collect_dirty_inputs` **skips a dirty chunk owning no cells** — which with a ring is a tile that still has sea to draw — and `mark_borders` steps chunk coords rectangularly, where `CLAUDE.md`'s rule is that only `lat_neighbour` steps a coordinate.  `mesh_chunks_touched` is ONE mechanism read two ways: as an edit it is the dirty rule, summed over the paint it is the tile list | `plans/25` § M2, `mesh_chunks.loft` |
| `@X074` | ⚠⚠ **colour is a UNIFORM and there is one mesh per palette kind** — moros's `chunk_mesh_mat(…, want_mat)` shape, and NOT a performance choice.  A flat-unlit frame drawn with a palette colour as a uniform **can only contain palette colours**, which is what keeps `@M002`'s exact classification alive; a per-vertex colour would interpolate across the triangle and every gradient pixel would land in `unknown` as a FAULT.  ⚠ So `RENDERER.md` § Open 3's *"flat unlit render mode for the gate"* is not a mode added later — it is the only way the mesher is written.  ⚠ `mesh3d::Vertex` carries no colour anyway, which is the shape agreeing rather than a gap | `plans/25` § M0, `RENDERER.md` § R4 |
| `@X076` | **the GL gate is a SEPARATE entry — `scripts/validate_gl.sh`, never rows in `scripts/validate.sh`.**  Plan 21 § R4's literal reading (*"validate.sh gains GL scenarios under xvfb"*) puts all **33** existing scripts behind an X server, and `RENDERER.md` § R0's probe went out of its way to prove the readback needs no display — *"or it could not run in CI either."*  ⚠ So a machine with no xvfb still runs the gate it can, and 654 headless measurements never wait on a GL context.  ⚠ Its consequence is a RULE: `.keys` has no GL verb (a `frame` command needing a context would drag all 33 scripts with it), so a fixture's expectations live in `src/gl_gate.loft` and a fixture with no case there is REFUSED by name rather than skipped | `plans/25` § M3, `gl_gate.loft` |
| `@X077` | ⚠⚠ **the GL gate's background is NOT a palette colour, and the gate's camera therefore looks at nothing but ground.**  A 3-D frame of a finite world is mostly background, so `other == 0` needs either a classified clear colour or a frame with no background in it.  ⚠ The first is REFUSED: an erased gap draws a HOLE (`@X075`), a hole shows the clear colour, and those pixels landing in `unknown` is the only thing that makes `@X075`'s named limit visible to a gate.  ⚠ So the clear colour is magenta — `palette_color`'s own "unknown kind" answer, outside the twelve buckets by construction as the palette grows — and `tests/gl/the-ground.keys` paints past the frustum, with `sea == 0` as the assertion that says so.  ⚠ A future fixture that cannot fill the frame cannot use `other == 0`; `an-island` is the worked example of what it asserts instead | `plans/25` § M3, `gl_gate.loft` |
| `@X078` | ⚠⚠ **a per-kind pixel count CANNOT see a mirrored world, so the GL gate also asks WHERE two uniquely-coloured hexes landed.**  Counts are permutation-invariant; `render_camera.loft` calls a mirrored world the failure that *"reads as a base that is its own reflection rather than as an error"* — so a gate built only of counts is blind to the defect that file is most afraid of (`@M027` measures it at 490.8 px past every green count).  The landmark is compared against **`camera_screen`**, the same function `@M022` measured the overview against the editor with, which chains drawn pixels → camera → software rasteriser and states `@X010` as something a frame can fail.  ⚠ A landmark must be **FLAT**: a column that stands draws its sides in the same colour, and those sides sit between the top face and the screen centre, so its centroid is not its centre — measured at **29 px** for a 5 m `wall_high` against **0.6 px** for a flat hex | `plans/25` § M3, `@M027`, `@X010` |
| `@X079` | ⚠⚠ **the fixed step's base unit is 1/3 of a MICROSECOND (3 000 000 a second), and microseconds were REFUSED on a measurement.**  A fixed step has to be an exact integer of some base unit or the construction collapses back into rounding — and dryopea's tick is 2/3 of a second, so `1e6 / 1.5` is **666 666.67** and no integer number of µs is it.  ⚠ `plans/26` § Open questions recommended **666 667 µs**, *"today's tick to within 1e-6 s"*, which *"moves nothing"*; measured, it moves **seventeen tests** (`@M031`).  ⚠ 1/3 µs is the COARSEST unit in which 2/3 of a second is whole — today's step is **2 000 000** units — so `TICK_SECONDS` derived from it is **bit-identical** to the `1.0 / 1.5` it replaced and there is ONE definition of the tick with no residue to document.  ⚠⚠ **The unit is the CONSUMER's choice, not the library's**: `clock_new` takes a step and the accumulator counts whatever the caller counts, because a game at 128 Hz cannot use 3 000 000 either (it is not divisible).  What `tick_clock.loft` promises is the IDENTITY, never a unit.  ⚠ The wall clock hands out µs and `clock_units_from_micros` is `* 3` — a multiplication, so the boundary `plans/26` § 1 found dryopea *"dividing the exactness away at"* is now exact | `plans/26` § L1, `tick_clock.loft`, `@M031`, `@X058` |
| `@X080` | ⚠⚠ **a `Bank` holds the CARRY and nothing else — the rate arrives per call and the WHOLE is a parameter.**  Plan 26 L2 replaced three hand-rolled *do-not-lose-a-fraction* sites (and the fourth that was missing, `@D003`) with one exact integer accumulator, and the two things it deliberately does NOT store are what make it usable.  ⚠ **The rate** is out because `@X061` says a rate is a property of a mover's CONDITION rather than of its class — *a damaged robot moves slower* — so `bank_gain(b, rate, elapsed, whole)` takes it every call.  ⚠⚠ **`whole` is out because loft defaults an omitted struct field SILENTLY** ([loft#914]) and dryopea builds `Enemy` from partial literals in a dozen places: a `Bank` carrying its own scale would default to 0 and make every one of them a mover that never moves, where a defaulted CARRY of 0 is a fresh bank and exactly right.  The price is one extra argument at three call sites inside `src/`, and no test ever writes it.  ⚠ **The rate is SCALED and the time is not**: time is already exact (`@X079`), a rate is an authored float, so `bank_rate` quantises to millionths — a tolerance on the RATE, never on the carry, which is the distinction the deleted epsilon blurred.  ⚠⚠ **The reciprocal form was refused on arithmetic**: storing *base units per whole unit* would make `Bank` into `clock_advance` with a variable step and need no scale at all, but `3 000 000 / 2.25` is 1 333 333.33 and `@M013` already sweeps 2.25 hex/s.  A rate scaled UP is exact for every authored number; a rate inverted is not — which is why `Bank` is not `TickClock` wearing a different name | `plans/26` § L2, `tick_bank.loft`, `@X061`, `@X079`, `@D003` |
| `@X081` | ⚠ **`vehicle_hexes_per_tick` is a CEILING that spends nothing, and it is a separate door from `vehicle_bank`.**  Once the player banks its ground, *how far may I point ahead* and *how far did I go* stop being one question: `play.loft::play_steer_reach` asks the first once per FRAME — sixty times a tick at 60 fps — so spelling it as a release would drain the player's carry just for holding a key down.  ⚠ The ceiling answers 2 cruising and 4 boosting at today's tick, exactly as the truncating version did, and answers **1** rather than 0 at a tick too short to guarantee a hex — which is the honest answer to *how far ahead*, and the one a release cannot give | `plans/26` § L2, `tick_bank.loft::bank_most`, `vehicle.loft` |
| `@X082` | ⚠⚠ **a `Timer` holds its `total` where a `Bank` may not hold its `whole` — and the rule that forbids one is the rule that permits the other.**  loft defaults an omitted struct field SILENTLY ([loft#914]), so the question a nested struct has to answer is *what does a defaulted instance MEAN at every site that builds a partial literal*.  For `Bank` the answer is fatal — a `whole` of 0 is a mover that never moves (`@X080`) — and for `Timer` it is exactly right: `total == 0` is UNARMED, which is what every `0.0` seconds field it replaces already meant (`recover: 0.0` is not recovering, `stand: 0.0` is free to walk, `lull: 0.0` is send now, `boost_left: 0.0` is not boosting).  ⚠ Holding `total` is also what lets ONE number answer BOTH directions — `timer_left` is `total − spent` — which is what deletes the direction problem rather than guarding it: there are no longer two accumulations to disagree.  ⚠⚠ **The family boundary was ATTACKED and held**: a one-shot built on `bank_gain` at `1/T` per second fires a SECOND time with nobody re-arming it, and its residue leaks into the next arming — a 5.0 s cooldown costs 8 ticks the first time and **7** the second.  Both failures are silent, which is why two small exact types beat one clever one | `plans/26` § L3, `tick_timer.loft`, `@X080`, `@M033` |
| `@X083` | ⚠⚠ **a POLICY is a DOOR beside the arithmetic, never a setting inside it — and the cap DROPS time rather than deferring or compressing it.**  Plan 26 L4 added the three things dryopea has no use for (a backlog cap, a rate scale, clock composition) because `plans/26` § FLEXIBLE's admission test is *could a consumer be tempted to hand-roll this?* and moros had already built two of them.  ⚠ **Each is a separate function over the ONE accumulator**, so `clock_advance` is untouched and `play_advance` stays uncapped — five entry points over five copies of the arithmetic would be § 2's seven-sites problem wearing a library's clothes.  ⚠⚠ **`clock_advance_capped` throws the excess AWAY**: the steps past the cap are not owed, so the frame after a stall answers 1 rather than a burst.  The version a driver writes by mistake clamps the answer and leaves the backlog banked — a DEFERRAL, which pays the stall off over the following frames and runs the simulation behind the wall for ever; both versions answer *fewer ticks* on the stalled frame, so the plan's stated control cannot tell them apart and the deferring driver is reproduced beside the shipped one as the real one (`@M034`).  ⚠ And nothing here can COMPRESS — the step is fixed and the door answers a COUNT — which is moros's own *a compressed catch-up is the variable-step bug wearing a bound's clothes*.  ⚠ A cap below 1 is clamped to 1 for `clock_new`'s reason: a simulation that can never advance is a mistake, not a mode; **pause is a rate of 0**.  ⚠ `clock_drive` is the honest exception — `n * step` at the call site is already exact because both sides are integers, so the door earns its place by NAMING the case rather than by fixing it, which is the opposite of `play_ticks` (wrong for 602 of the first 1000 `n`) and is asserted as such | `plans/26` § L4, `tick_clock.loft`, `@M034`, `19-P1` |
| `@X084` | ⚠⚠ **a clock's RATE is a RATIONAL, applied at ONE private site, to every DURATION door and to NO count door.**  `num / den` with the wall remainder carried in `[0, den)` — pause is `0 / 1`, half-speed `1 / 2` — so a rate that does not divide loses nothing for ever, where the hand-rolled `elapsed * 0.3333…` a consumer reaches for drifts against a rate it could not state in the first place (measured: a wall minute at a third of speed is **30** ticks exact and **29** truncated, `@M034`).  ⚠ It is `tick_bank.loft` § THE RATE IS SCALED AND THE TIME IS NOT one family over, with the opposite conclusion about exactness: a mover's rate is authored in `numbers.json` as a float and must be quantised (`@X080`), a clock's rate is authored by a KEYPRESS and can be stated exactly.  ⚠⚠ **The count door is exempt and that is the load-bearing half** — a script's `tick 30`, a turn being committed and a rollback re-simulation have no wall clock to slow down, and a paused game unable to run its own tests would be a mode flag wearing a rate's clothes.  ⚠ **A second accumulator, and it is a different QUESTION rather than a second copy of one**: `banked` carries sim time toward the next step, `rate_carry` carries wall time toward the next sim unit; folding them by scaling `banked` would make `clock_banked`'s UNIT change with the rate, and it is a save/restore value.  ⚠⚠ **A defaulted rate of `0 / 0` reads as UNSCALED, which is this plan's THIRD answer to one loft rule** ([loft#914], a silently defaulted field): a `Bank` may not hold its `whole` because a defaulted 0 is a mover that never moves (`@X080`), a `Timer` MAY hold its `total` because a defaulted 0 is unarmed (`@X082`), and here the defaulted value IS *the behaviour you already had* — `plans/26` § The two rules under all of it, stated as data rather than as prose.  ⚠ A negative `num` is clamped to 0 rather than rewinding: time going backwards is dropped everywhere else in the clock, and a reversing rate would need every consumer to own an inverse tick | `plans/26` § L4, `tick_clock.loft`, `@X080`, `@X082`, `@M034` |
| `@X085` | ⚠⚠ **the ALPHA is the clock's and the POLICY is the renderer's — and an eased follow camera never owned the mover in the first place.**  Plan 26 L5 shipped `clock_alpha` (the bank as a fraction of the step, half-open by construction) and `play_alpha`, and **no interpolation policy at all**.  ⚠⚠ **The measured reason it is not the clock's call**: three policies, three prices on three different axes — `lerp(prev, cur, alpha)` is smooth and **one whole step behind for ever** (2.598 m here), `cur + alpha·(cur − prev)` is **exact to 9.5e-16 m** while the velocity holds and jumps a whole step's travel whenever it changes (2.598 m starting, 2.533 m stopping), and a first-order ease needs no previous state, lags between them and is the only one whose picture depends on the frames it was drawn on.  **Which is right is a function of how long the STEP is**, so a clock that picked one would be wrong for every consumer whose step is a different length (`@M035`).  ⚠⚠ **And the second half is about WHERE it is applied**: the shipped ease does not remove a lattice mover's jump, it moves it off the world and onto the mover, so alpha on the mover alone leaves a seventh of it behind — *the mover and the camera's target are one decision, not two*.  ⚠ Nothing is built today because nothing DRAWS an entity today; a previous position per drawn thing would be a field nobody reads, and [`plans/20`](../plans/20-entity-art/README.md) inherits both halves | `plans/26` § L5, `tick_clock.loft`, `play.loft`, `@M035`, `@M023` |
| `@X086` | ⚠⚠ **the timing arithmetic is a PACKAGE in `loft-libs-game`, and the admission test that put it there is *every game needs this* rather than *two games wrote it*.**  Plan 26 L6 moved `tick_clock.loft`, `tick_bank.loft` and `tick_timer.loft` out of `src/` unchanged, plus the camera's ease out of `render_camera.loft`, into `fixstep 0.1.0` — zero dependencies, 13 tests, one per door.  ⚠ **A move, not a rewrite**, and that is the gate: dryopea's **1322 tests and 654 measurements are green and UNMOVED** across it.  ⚠⚠ **`loft-libs-game` rather than a standalone repo** (project owner, 2026-08-17: *"something like this is needed for every game that is built"*) — the surveyed evidence found only ONE other duplicated candidate (`approach`, which ships inside the package), so a themed repo looked like over-building on duplication alone; the owner's test is the stronger one and that chunk already exists for *runtime game services … consumed by every game*.  ⚠ The chunk's README had advertised the slot since bootstrap — its `time/` row reads *"frame counter, dt, scheduling"* while `time` shipped as date arithmetic — so this fills a package that was planned and never landed.  ⚠ **`moros_map` is NOT a second tenant**: it is a world model and belongs beside `hex_world` in `loft-libs-world`.  ⚠ **`tick` was refused twice over** — `ticks()` is a loft builtin, AND moros already has a `tick` module that is a Walker rather than a clock.  ⚠⚠ **The example half is gated and it caught itself**: a tag in a section header is separated from its `fn` by a blank line, which breaks the binding, so all thirteen citations were dangling while looking correct | `plans/26` § L6, `loft-libs-game/fixstep`, `@X079`, `@X080`, `@X082` |

### play — progression ([`PROGRESSION.md`](PROGRESSION.md))

| code | decision | owner |
|---|---|---|
| `@X016` | ⚠⚠ **SUPERSEDED 2026-08-26 by `@X111`** — ~~the progression is SKILL, not stats~~.  It was a session's derivation, not the owner's design: *"progression should not be about player skill … there can be multiple dimensions"*.  ⚠ What survives is that **what the player KNOWS and DECIDES is the dominant axis** (`@X117`) — a different claim, about information rather than dexterity | `PROGRESSION.md` § P0, § P1 |
| `@X017` | ⚠⚠ **SUPERSEDED 2026-08-26 by `@X116`** — ~~the player's vehicle must not get faster~~.  The same over-correction: what constrains a vehicle upgrade is the FRICTION fence, not a ban.  ⚠ A vehicle that carries more, climbs better or sees further passes; a vehicle that FIGHTS still does not (`DESIGN.md` § 8) | `PROGRESSION.md` § P3, § P6 |
| `@X018` | the **landscape is the school** and the fiction already made it safe — both non-human tiers open as maintenance.  ⚠ Still true, and after `@X117` it is where the DOMINANT axis is learned rather than where a dexterity is drilled | `PROGRESSION.md` § P1, § G2 |
| `@X019` | the **base layout is the exam** — ⚠ **and since 2026-08-26 it is examined on three axes**: what the player KNOWS (a sealed wall doubles the clock, a gate buys nothing), what they can FLY, and what their crew can SERVICE.  The measured racing line is intact; it is no longer the only reading | `PROGRESSION.md` § P7, `@M004` |

### run — exploration and the session clock ([`EXPLORATION.md`](EXPLORATION.md))

| code | decision | owner |
|---|---|---|
| `@X020` | **exploration IS scouting** — already *the* progression activity; this design assembles rather than adds a pillar | `EXPLORATION.md` banner |
| `@X021` | **the run already opens with a sortie** (`wave_provoke_step`), so "explore earlier" is CONTENT on a trip the player already takes | `EXPLORATION.md` § X2 |
| `@X022` | ⚠⚠ **the game WAITS** — an unlimited recon window the player ends deliberately by poking a marker | `EXPLORATION.md` § X2b |
| `@X023` | **the four robot classes gate the intel layer** — until they exist every wave is the same wave and a sortie predicts nothing.  ⚠ One row each in `numbers.json` + one branch in the damage-to-wall lookup.  ⚠ **HALF DONE** — the wall-damage axis shipped as `23-K0` and was exactly that cheap; the SPEED axis is `23-K2` and is not a row (see `@X053`) | `EXPLORATION.md` § X2b, `plans/23` |
| `@X053` | ⚠ **the harvester's role is what it CARRIES** — its body is `RUBBLE_CARGO` and cargo pays **3×**, so the one hole `DESIGN.md` § Small robots left open is filled by `body_source` → `loot_rate`, two lookups that already composed.  ⚠ Chosen against the genre test: at 90 points a body, letting one through and collecting it later is a real alternative to killing it at the wall | owner, 2026-08-15; `plans/23` § Open questions 1 |
| `@X054` | ⚠ **`robot` is not retired and is not one of the four** — the small robots APPEND as kinds 3-6 and the baseline keeps 1.0 HP/s, which is what let four classes land without moving one of 1 094 tests or 520 measurements | `plans/23` § K0 |
| `@X055` | ⚠⚠ **a wave's SIZE is SUMMED from its parts, never stored** — a wave is a flat `vector<WavePart>` keyed by wave index, so *the parts sum to the count* is the definition rather than an invariant.  ⚠ This **DELETES** `plans/23`'s own named negative control (*"a mix whose parts do not sum to the wave's count is refused at parse"*): the check only exists if the count is a second fact beside the parts, which is the shape `carry.loft` refuses.  ⚠ `wave_schedule_of(vector<integer>)` keeps its meaning — N waves of REGULARS — which is what let composition land with **569 measurements unchanged** | `plans/23` § K1, `waves.loft` § A wave is PARTS |
| `@X056` | **`schedule` arms and `compose` fills** — two `.keys` verbs rather than one grammar, because `schedule 12 12` already means *two waves of twelve* and a form that also took class names could not tell a wave boundary from a group boundary.  ⚠ `compose` REPLACES a wave, so re-running a fixture reaches the same state; and a `schedule` line AFTER a `compose` wipes it, which is why `emit.loft`'s order is a requirement | `plans/23` § K1 |
| `@X057` | ⚠ **`WaveFile` is deliberately NOT given composition** — nothing in `src/` loads it, and the shape it would need is `vector<Struct>` through a `text as`, which is the cast that hangs and miscompiles to zero entries on native.  Composition is authored in the `.keys` vocabulary, where dryopea authors everything else about a run | `plans/23` § K1, `plans/23` § Open questions 3 |
| `@X058` | ⚠⚠ **the tick's duration is no longer DERIVED, it is CHOSEN** — an enemy banks `speed × tick_seconds` and steps when a whole hex is due, so `TICK_SECONDS = 1/ENEMY_SPEED_HEX_PER_SECOND` is now what HOLDS the timestep at one regular's hex rather than what forces it.  ⚠ The expression stays because changing it moves all 569 measurements for a reason that is not a defect | `plans/23` § K2a, `spawn.loft` § What a tick is worth |
| `@X059` | ⚠ **a hex the ground refuses is SPENT, not re-banked** — the OPPOSITE of `helper_bank`, and deliberate: a helper parked at a wall keeps accumulating so its average stays a rate, but an enemy stopped by a wall is BESIEGING, and storing the hexes would let a robot that chewed a breach for ten ticks cross the base the moment it opens | `plans/23` § K2a |
| `@X060` | ⚠ **`enemy_bank` takes its SPEED as an argument**, where `helper_bank` reads a constant — because 1.5 hex/s is one of the few speeds at which the rounding guard cannot fire, so a bank that read the constant could only ever be tested at the value that hides its own guard | `plans/23` § K2a, `@M013` |
| `@X061` | ⚠ **the class lookup lives at the CALL SITE, not in the bank** — `enemy_tick` asks `enemy_speed(e.kind)` and hands the number over, so `enemy_bank` still knows only a rate.  The reason has outlived `@X060`'s: `DESIGN.md` § Speed must NOT be tied to the tick wants speed to vary *within a life* (*"a damaged robot moves slower"*), which makes the number a property of an enemy's CONDITION rather than of its class — so the one line that decides pace has to be somewhere a condition can also be read | `plans/23` § K2b, `spawn.loft` § How fast each class arrives |
| `@X062` | ⚠ **harvester and builder get NO speed row**, and that is `numbers.json`'s DELTAS shape rather than an omission: a role that lists no field takes the regular's.  Giving all four a row would invent two tunables to say *unchanged* | `plans/23` § K2b, `NUMBERS.md` |
| `@X063` | ⚠⚠ **2.25 and 3.0 hex/s were refused for a ROUNDING reason, not a design one** — both are *"quite a bit faster"* and both make `speed × tick_seconds` exact, hiding `ENEMY_PROGRESS_EPSILON` exactly as 1.5 does (`@M013`).  Picking one would have left the whole roster unable to see its own rounding.  ⚠ The general rule: **when a constant's guard is invisible at the shipped value, the next value shipped should be one that can see it** | `plans/23` § K2b, `@M013`, `@M017` |
| `@X064` | ⚠⚠ **the equal-distance SIDESTEP is refused inside plan 23 and priced instead** — `@M018` measures a mixed wave collapsing to its fastest class because only three robots can ever reach the wall, and the fix is a besieger spreading ALONG the face.  ⚠ It is a second steering rule, so it is a PLAN; and a measurement phase that changed the thing it was measuring would have measured nothing.  ⚠ The general rule: **when a measurement finds the cause of what it measured, its job is to price the fix, not to apply it** | `plans/23` § K3, `@M018`, `ENEMY_MOVEMENT.md` § The siege front is three hexes wide |
| `@X024` | **a find is a BUILD ACCELERANT**, and what decays is *the opportunity to use it* — so it must be found early | `EXPLORATION.md` § X2c, `@M006` |
| `@X025` | **the PERMIT is the run clock**, and the cordon's teeth are at the exit.  ⚠ Expiry costs the **CARGO**, never the run — § 14 has no fail screen | `EXPLORATION.md` § X2d |
| `@X026` | a find is **one marker row + one cargo row**, and nothing else.  ⚠ A find that needs a subsystem has become an economy | `EXPLORATION.md` § X4 |
| `@X027` | **every find opens a fight**, and the fight is a wave that already exists — no new mover | `EXPLORATION.md` § X5 |
| `@X028` | **the first find is a stranded helper** — it needs no code at all, and it is a build accelerant whose value compounds | `EXPLORATION.md` § X7 |
| `@X029` | **what persists is INTEL** — a per-node `found` flag, not revealed pixels.  ⚠ No fog-of-war layer | `EXPLORATION.md` § X8 |

### run — the setting and the seed hand-over ([`SETTING.md`](SETTING.md), [`MATERIALS.md`](MATERIALS.md))

⚠ Folded 2026-08-26 from [`../archive/seed-notes.md`](../archive/seed-notes.md)
and [`../archive/gameplay.data`](../archive/gameplay.data); routed block by
block in [`DESIGN_HISTORY.md`](DESIGN_HISTORY.md) §§ 4-5.

| code | decision | owner |
|---|---|---|
| `@X099` | ⚠⚠ **the jammer forbids remote operation, and that is the game's charter** — *"very little communication with the surface possible during a mission, so there has to be personnel below to service the different systems"*.  The equipment that makes the robots beatable is what puts humans on the ground, so every servicing loop the project has measured exists because of one sentence in the recruitment call.  ⚠ It is what refuses a power grid, automated transport, and any comms upgrade that works under the scrambler | `SETTING.md` § The recruitment |
| `@X100` | **the pollen is the environmental cause of the repair economy** — aggressive to humans AND machines, so repair is a condition rather than an event.  ⚠ It must stay a CONSTANT and never become a weather event with a timer, or it is a lean-back mechanic | `SETTING.md` § The pollen |
| `@X101` | ⚠ **rock blocks radio**, which is why the AI is a surface power, why the natives were never found, and what the player's 25-hex bubble is imitating.  ⚠ It is also why a cave is a hiding place rather than a shortcut — a place the enemy cannot reach is a place the game does not happen | `SETTING.md` § Rock blocks radio |
| `@X102` | ⚠⚠ **the knowledge tree is the one accumulating progression that passes the genre test** — ~55 facts in per-faction arcs.  ⚠ The boundary: knowledge changes what the player can **ATTEMPT** and what the map will SHOW them, never how well they do it.  *Cave entrances* makes a scan exist; it does not make scanning better | `SETTING.md` § The knowledge tree, `PROGRESSION.md` § P4 |
| `@X103` | ⚠⚠ **REVERSED 2026-08-26 by `@X111`** — ~~the 2023 RPG layer is refused~~.  **The owner's ruling: *"the RPG part here will win"*.**  ⚠ Recorded rather than deleted because the refusal stood for one day and the mistake is instructive: it read *a stat* as *a route to winning by personal power*, which `@X118` separates | `PROGRESSION.md` § P0 |
| `@X104` | ⚠⚠ **a POWER GRID is refused for the player's base** — a wire replaces a drive, and `plans/17` § T3 measured that the drive IS the game (two shuttling helpers clear 205 robots; the same two parked lose the base).  ⚠ Generators are fine: a source with a position is the existing mechanic with new flavour.  ⚠ The **enemy** may have the full grid — a power line between installations is one more edge to cut | `MATERIALS.md` § Power |
| `@X105` | ⚠ **automated transport is refused for the player and welcomed for the enemy** — *"maximize automatic transport of materials"* removes the trip.  `ROBOT_ECONOMY.md` § 3 already makes a rail a route to cut, which is the version that adds a decision instead of deleting one | `MATERIALS.md` § What this design does NOT do |
| `@X106` | ⚠ **a material tree passes the genre test only because materials have WEIGHT and are SOMEWHERE ELSE** — getting one is a trip.  A tree that resolves into a menu is the failure mode.  ⚠ Ship **three** materials, not the 2023 catalogue's 34; the rest arrive when a mechanic asks for one by name | `MATERIALS.md` § The governing rule, § Open questions 3 |
| `@X107` | ⚠ **the AI core is the one part that cannot be manufactured, only taken** — and a recycler must never break one down, or the highest-value thing in the game becomes a quantity | `MATERIALS.md` § Parts |
| `@X108` | ⚠⚠ **a trap that does not auto-reset passes the design test outright** — placed in advance, fires once, then somebody drives out mid-wave to re-arm it.  Mechanically a black tower restored by a standing vehicle (`plans/17` T1) with the cost moved to the front | `MATERIALS.md` § Towers, `plans/ROADMAP.md` Tier B |
| `@X109` | ⚠ **a FLYER is blocked and a wall-climbing SPIDER costs one row** — `ENEMY_MOVEMENT.md`'s *ONE AI, per-class DATA*: a flyer needs its own mover and deletes the wall, while `can_climb` already takes the limit as a parameter | `DESIGN_HISTORY.md` § 4c |
| `@X110` | ⚠ **the six damage TYPES stay six** — the 2023 sheet's twelve are not an upgrade to `DESIGN.md` § Damage TYPE's triangle.  ⚠ Two of them contribute: `falloff` is *range is a PROFILE, not a number* named, and `poison` is *"ignored by machines"*, i.e. a purely anti-insect axis | `DESIGN_HISTORY.md` § 5d |
| `@X111` | ⚠⚠ **the crew are CHARACTERS — the 2023 RPG layer is ADOPTED** (owner, 2026-08-26): twelve skills, six statistics each bonusing four of them, endurance pools that rest restores, backgrounds and classes.  ⚠ `DESIGN.md` § 9 has carried a per-helper id since plan 14 *"so future skills hang off existing characters"* — the data model was already waiting | `PROGRESSION.md` § P2 |
| `@X112` | ⚠⚠ **a skill SCALES A NUMBER THAT ALREADY EXISTS** — `build` scales helper-seconds (the bottleneck `DESIGN.md` § 13 already names), `repair` the 20 s standing clock, `scout` the detection radius, `scrounge` `loot_rate`.  ⚠ So no skill introduces a mechanism, and the layer lands one skill at a time against numbers that are already measured | `PROGRESSION.md` § P2 |
| `@X113` | **endurance POOLS, spent by work and restored by rest** — the crew-side version of the tower's charge.  ⚠ It is what makes `MATERIALS.md`'s *living quarters* a real building: a sortie long enough to need rest must build for it.  ⚠ And *"personnel can join competitors when not treated well"* makes neglect a run-level consequence with a face on it | `PROGRESSION.md` § P2d |
| `@X114` | **the 28 upgrades are design material** — reversed with the rest.  ⚠ Two still need a ruling and it is `@X118`'s rather than a blanket one: the **laser communicator** survives only as a LINE-OF-SIGHT relay (a general radio deletes `@X099`), and the **auto pilot** keeps its authored drawback because it automates the axis the player enjoys | `PROGRESSION.md` § P3 |
| `@X115` | ⚠ **the perspective rule** — the world is seen through a person, and switching to another happens only at the main communication terminal.  ⚠ With `@X111` adopted this stops being a curiosity: without it a skilled crew is a spreadsheet the player watches, and with it the switch costs a drive back to the core mid-wave.  ⚠ Designed, not decided — it changes the control model (`DESIGN.md` § 8, § 9) | `PROGRESSION.md` § P8 |
| `@X116` | ⚠⚠ **the model is BLUE PRINCE** (owner, 2026-08-26): information and the player's own build choices DOMINATE progression, *"but there is still a lot of room for game progression that gradually makes traversing/building easier"*.  ⚠ Two progressions that never compete because they answer different questions — and the practical consequence is that **the friction-reducers can be generous** | `PROGRESSION.md` § P0 |
| `@X117` | ⚠⚠ **the dominant axis is what the player KNOWS and DECIDES**, not what they can execute — laying out a base is dryopea's DRAFT.  ⚠ *A sealed wall doubles the clock and a gate buys nothing* (`@M004`) is the purest example: no upgrade grants it, no stat substitutes for it, and a player who has it plays differently on day one of a new run.  ⚠ Execution (boost lines, taking a line under pressure) is real and is the MINOR kind | `PROGRESSION.md` § P1a, § P1b |
| `@X118` | ⚠⚠ **the fence is FRICTION vs ANSWERS** — *does this reduce the cost of acting on what the player knows, or does it supply what they do not know?*  A shortcut saves you steps; it never tells you the combination.  ⚠ **Build speed, repair speed and detection radius all pass**; a layout the game recommends, a readout that NAMES the incoming composition, or salvage without the trip all fail.  ⚠ The failing column is not *too strong* — it is **doing the player's thinking** | `PROGRESSION.md` § P6a |
| `@X119` | ⚠ **and the player stays SMALL** (owner, 2026-08-14) — no route to *become strong enough and win*.  ⚠ Much narrower than the refusal it replaces: it forbids a weapon skill that lets one person hold a perimeter alone, and hacking that converts a boss into a bodyguard.  **It does not forbid a stat** | `PROGRESSION.md` § P6b, `DESIGN.md` § And the DEEP layers |
| `@X120` | ⚠⚠ **A RETURNING PLAYER MAY SKIP THE RAMP, AND NOTHING MAY STOP THEM** (owner, 2026-08-26) — *"it is still possible to make huge progress in the first mission when a player decides to restart the game after a long campaign"*.  ⚠ So **the first base may not be balanced for a naive player**, and content may not be gated on progress the player has already earned in their head.  ⚠⚠ Its corollary is a rule for every unlock: **prefer FOUND over AWARDED** — `DESIGN.md` § Scouting already finds new tower types on the map, and a veteran knows where to look, where *"clear three missions"* is impermeable to knowledge | `PROGRESSION.md` § P1c |
| `@X121` | **the second progression's job is SMOOTHING** — *"after doing many scenarios the game helps the player to get to content/actions easier later in the game"*.  ⚠ It is not difficulty compensation and not a power curve: it is what stops a long campaign spending its minutes on problems the player solved twenty sorties ago | `PROGRESSION.md` § P0c |
| `@X122` | ⚠⚠ **the RULES are learnable, the INSTANCE is not** (owner, 2026-08-26) — *"enough random elements in each scenario that exploration stays a big part of it … you roughly know what to expect but cannot predict everything"*.  ⚠ Without it `@X117` eats itself: a dominant knowledge progression in a FIXED world is solved after enough runs.  ⚠⚠ **Vary the instance, never the rules** — where the ridge is, which classes are in the wave, which tree withered; never what a sealed wall is worth or how fast a miner chews (which is also why `numbers.json` is global and not per-map).  ⚠ And a veteran must be able to form good expectations and be surprised at the MARGIN — too much randomness destroys the dominant axis from the other side | `PROGRESSION.md` § P1d |
| `@X123` | ⚠ **scouting converts rules-knowledge into instance-knowledge, every run** — which is why exploration never goes stale and why the veteran scouts **faster and better rather than less**.  ⚠ It is also what makes `scout` / detection radius a legitimate friction-reducer under `@X118`: they make the conversion cheaper and convert nothing on their own | `PROGRESSION.md` § P1d, `EXPLORATION.md` |
| `@X124` | ⚠⚠ **the two layers are a LOOP — practice raises the STATISTIC, and the statistic permeates** (owner, 2026-08-26): a skill rises fast with use, its two statistics rise slowly, and those lift **six other skills** each.  ⚠ Measured off the 2023 data rather than retyped: 12 skills, 6 statistics, every skill bonused by exactly 2 and every statistic bonusing exactly 4, **diameter 2** — so no crew member ever dead-ends and the six-helper cap does not force redundancy.  ⚠⚠ It passes `@X118` automatically, because use-based advancement can only reward work the player already decided to do; and it is `@X121`'s smoothing **with no dial on it** | `PROGRESSION.md` § P2a |
| `@X125` | ⚠⚠ **TEMPLATES, never rerolls** (owner, 2026-08-26) — *"I do not like the system where players have to do many rerolls to get the optimal NPC … we give them templates instead that can be optimized by actually letting the NPC perform related tasks"*.  ⚠ A reroll loop is `@X118`'s failure mode wearing a dice cup: the player pays time for the game to hand them a stat block.  ⚠⚠ **And do not randomise which templates are OFFERED** — that re-invents it one level up, and `@X122` does not license it, because a hiring pool is not a place you go and LOOK at | `PROGRESSION.md` § P2b |
| `@X126` | ⚠ **practice banks at the SCRAMBLE** — gains accrue during a sortie and count for a helper who boarded before force-launch, over the cargo manifest `DESIGN.md` § 14 already keeps.  ⚠⚠ So leaving somebody behind now costs their **experience** as well as their body, which sharpens the scramble decision rather than softening it — and it needs no new mechanism | `PROGRESSION.md` § P2a |
| `@X127` | ⚠ **advancement must not reward a NO-OP** — a use-based system invites grinding repairs on an undamaged tower.  dryopea's existing mechanics already enforce most of it (a **firing tower refuses repair**, salvage decays, the permit clocks the run), and the question to ask of every new skill is whether its practice can be repeated at zero cost | `PROGRESSION.md` § P2a |
| `@X128` | ⚠ **`combat` and `scout` are statistical TWINS** — both observe+stamina, the only collapsed pair in the lattice, which is why both permeate to 5 where every other skill reaches 6.  ⚠⚠ **dryopea should drop `combat`**: `DESIGN.md` § 8's noncombatant player leaves it almost nothing to scale, while `scout` scales the detection radius.  Dropping it orphans no statistic and changes no other skill's permeation.  ⚠ And a NEW skill should take one of the **four free pairs** (agility+stamina, charisma+observe, observe+plan, stamina+tinker) rather than duplicate an occupied one — duplication is how the twins happened | `PROGRESSION.md` § P2a |
| `@X129` | ⚠⚠ **the crew REMARK on the world, coloured by their past job** (owner, 2026-08-26) — an ex-miner, an ex-security officer, an ex-handyman and an ex-scout land with different skills and say different things.  ⚠⚠ **A remark POINTS, it never CONCLUDES**: *"soft ground on that ridge"*, never *"build here"*, and never `@X118`'s failing row (a layout the game recommends, a readout that NAMES the incoming wave).  ⚠⚠ **The crew are PARTIAL SENSORS** — each right about their own domain and blind to the rest, so following one exclusively is how a base goes wrong and the synthesis stays with the player (`@X117`).  ⚠ It PRESERVES the trip rather than removing it: a remark is a reason to go and look.  ⚠ And it is the skill sheet made diegetic — the player reads the roster by noticing who speaks up about what, with no character screen | `PROGRESSION.md` § P2c, `DESIGN.md` § HUD, § 9 |
| `@X130` | ⚠⚠ **crew remarks are BLOCKED on text, and they are the first designed feature that is** — `@X097` says `draw_text` rasterises through a `#native` call unavailable under `loft test` and needs a font file this repo lacks, which is why the wallet is seven-segment rectangles.  ⚠ Everything else in the design routed around it (a colour, a shape, a pulse); a spoken line cannot.  **So the prerequisite is a font in the repo plus `draw_text` reachable under `loft test`**, or the remark is a thing no test and no `snap` can see.  ⚠ The buildable interim is a speaker and a subject drawn without words — the crew vehicle indicating, the subject outlined — strictly weaker, and gateable by pixels | `PROGRESSION.md` § P2c |
| `@X131` | ⚠⚠ **a relationship is a LEDGER of what happened, never an affinity bar** (owner, 2026-08-26) — *"a shared history gets them together"*.  ⚠ It is the SAME MECHANISM as `@X124`: the crew get better at what they DID, and closer through what they WENT THROUGH — one system, two readouts.  ⚠⚠ **The player never raises it**; they run sorties and the crew remember, so there is nothing to farm (`@X125`) and no bar to draw (`DESIGN.md` § HUD).  ⚠ Their only influence is indirect and positional: **who they send together, and who they come back for** | `PROGRESSION.md` § P2e |
| `@X132` | ⚠⚠ **the history already EXISTS** — who retrieved whom (`DESIGN.md` § 9, shipped in `plans/14`), who was left behind at force-launch (§ 14, *"the cost of haste"*), who boarded (the cargo manifest `@X126` already banks on), which bases fell with you both in them.  ⚠ So the feature adds a record and not a simulation.  ⚠⚠ **And § 9's STRANDED HELPER becomes the campaign's best story beat with nothing written**: you left them because the wave was on the core, and going back is a decision about a person you know — the signature mechanic generating its own drama | `PROGRESSION.md` § P2e, `DESIGN.md` § 9 |
| `@X133` | ⚠ **a relationship MAY have a mechanical effect, through the lattice that already exists** — `social` is a skill and `charisma` a statistic, so a bond is the § P2a loop with shared work as the practice.  ⚠⚠ The passing form is narrow and it is all COVERAGE: two people who have worked together **hand a job over without re-walking it** (`plans/17` § T1 already banks repair ON THE TOWER for exactly that reason).  ⚠ What fails `@X118`: a bonded pair that unlocks an ability, a loyalty THRESHOLD granting a bonus, or a relationship GATE on any content | `PROGRESSION.md` § P2e |
| `@X134` | ⚠ **keep COUNTS and LANDMARKS, never a log** — an event log per crew pair grows for ever, which is the shape `@X095` already refused for a different subject.  ⚠ A few counts (sorties together, retrievals, abandonments) plus two or three landmark events kept **by name**, so a fifty-sortie campaign costs a fixed handful of fields per pair and a remark still has something concrete to point at.  ⚠⚠ Prerequisites are real: **per-campaign persistence** (ROADMAP Tier D, not shipped) and **text** (`@X130`), which a bond needs more than a remark does | `PROGRESSION.md` § P2e |
| `@X135` | ⚠⚠ **remarks fire in OFF-TIME, gated on the player's own state** (owner, 2026-08-26) — *"the gentle nudge to a player that feels lost"*.  ⚠ A player getting on with it hears nothing; a player who has stalled hears one line.  ⚠⚠ **It satisfies `@X120` for free**: a veteran who never stalls never hears a hint, so the tutorial is skipped without a skip button, a difficulty setting or a toggle.  ⚠ The off-time is a phase that already exists — `@X022`'s unbounded recon window, which the player ends deliberately by poking a marker | `PROGRESSION.md` § P2c |
| `@X136` | ⚠⚠ **SILENCE must be a real outcome, and it is the DEFENCE rather than modesty** — *"they do not hold all the answers"*.  If the crew always produce a useful line when the player stalls, **stalling becomes the way to get answers** and a player will stall deliberately: `@X118` violated through a back door, with waiting as the currency.  ⚠⚠ **And the trigger is the absence of PROGRESS, never idleness** — in dryopea *standing still is work*: a parked player is repairing (`plans/17` § T1, repair is a POSITION), so an idle detector would interrupt the most important job in the game.  ⚠ Bias the window LONG: firing early talks over a thinking player, firing late costs nothing because they solved it themselves.  ⚠ It may read the CAMERA where `@X033` forbids the simulation to — a nudge is presentation — provided nothing the simulation reads back is fed from it | `PROGRESSION.md` § P2c |
| `@X137` | ⚠⚠ **THERE IS NO TUTORIAL** (owner, 2026-08-26) — *"simple/visible enough controls that a player can just find by playing around"*.  No tutorial mission, no first-run overlay, no tooltip layer.  ⚠⚠ It is affordable because of `DESIGN.md` § Position triggers: **most of the game has no key to learn at all** — driving over loot picks it up, driving to a tower repairs it, standing on a spawn marker starts the run.  ⚠ Play mode is **five keys** (WASD, E, Q, Shift) plus the mouse | `DESIGN.md` § There is NO TUTORIAL |
| `@X138` | ⚠⚠ **the recon window IS the tutorial sandbox, and nobody built it for that** — `@X022`'s unbounded wait means a new player can wander, press every key and drive into everything with **nothing punishing them**, because `SETTING.md` § Nobody is attacking anybody makes the world safe until the player interferes and `wave_provoke_step` starts the run only when they say so.  ⚠ So three of a tutorial's four jobs were already answered (controls by position triggers, feedback by the diegetic HUD, safety by the wait) — and the fourth, **what to do first**, is what `@X129`'s remarks are for, which promotes them from a convenience to **the onboarding** | `DESIGN.md` § There is NO TUTORIAL, `PROGRESSION.md` § P2c |
| `@X139` | ⚠⚠ **the key table is a DESIGN BUDGET, and a gate already enforces it** — every key is a thing the player must discover unaided, so a new binding needs an argument rather than a row.  `tests/09_i1_bindings.loft` pins the ROW COUNT of `bindings.loft`'s one table, written as a regression check and now carrying the design meaning too.  ⚠⚠ **And it adds a TEST for every future mechanic**: *can a player find this by playing around?*  A mechanic that needs explaining needs **redesigning**, because there is nowhere left to explain it | `DESIGN.md` § There is NO TUTORIAL |
| `@X140` | ⚠ **two named discoverability hazards** — **Q** (wall-paint toggle) is the sharpest: § 11 already calls it *"acknowledged exception to the spatial principle"*, and **building is the biggest missing mechanic**, so a player who never presses Q never plays the actual game.  ⚠ **Shift** (boost) is second — `plans/13` § V4 measured it as the only way out of a sealed base — but the hazard is **ordered**: sealing yourself in requires a wall, which requires Q.  ⚠ Q's mitigation today is the red vehicle tint, obvious *once found*; finding it is what a landing remark is for | `DESIGN.md` § There is NO TUTORIAL |
| `@X141` | ⚠⚠ **a simple input scheme is NECESSARY and not SUFFICIENT** (owner, 2026-08-26) — *"not guarantee that the player will be engaged and have a clear view about what to do.  So that is where the helpers come in"*.  ⚠ Three distinct failures, and controls only reach the first: *I don't know how* (position triggers), *I don't know what is worth doing* (the crew, as information), *I have stopped caring* (the crew, as **engagement** — a different problem a control scheme can never reach).  ⚠⚠ **A silent world is where players drift**: a crew member remarking on where you stand is the world NOTICING you.  ⚠ And across a campaign it is `@X131`'s shared history that makes the voice worth hearing — **the remarks and the relationships are one system doing two jobs**, information and attachment | `DESIGN.md` § There is NO TUTORIAL, `PROGRESSION.md` § P2c, § P2e |
| `@X142` | ⚠⚠ **the helper with NO TASK is the one who speaks** (owner, 2026-08-26) — the player gives the orders, so an unassigned worker reporting in is what a foreman hears, and `DESIGN.md` § 8's management loop supplies its own feedback channel.  ⚠⚠ **It supersedes `@X136`'s progress heuristic and is strictly better**: it reads a task list that already exists rather than six counters and a tuned window, it **cannot misfire on a parked player who is repairing**, and it is a fact the player can see and act on.  ⚠⚠ **It self-calibrates** — assign everyone and there is silence; land with nobody assigned and the whole crew is idle, so `@X135`'s onboarding moment falls out of the rule instead of being special-cased.  ⚠ Idleness picks WHO and WHEN; `@X129`'s skills pick WHAT, so the ex-miner says *"there's ore on that ridge"* rather than *"I am idle"* | `PROGRESSION.md` § P2c, `DESIGN.md` § 9, § 11 |
| `@X143` | ⚠ **ONE speaks, never six** — a chorus of idle helpers is the opposite of *not intrusively*; pick the one whose domain best matches where the player is standing.  ⚠ And an idle helper speaks **when there is something new in their domain**, never on a repeat timer, so `@X136`'s silence still governs.  ⚠⚠ **Assigning work is the in-fiction OFF SWITCH**: a player who finds the crew talkative silences them by doing the thing the remarks were nudging toward — the remedy for the hint system is the play it wanted.  ⚠ Busywork-to-buy-quiet is self-limiting, because **helper-seconds are the real bottleneck** (`DESIGN.md` § 13), so a wasted order costs exactly what the player was short of | `PROGRESSION.md` § P2c |
| `@X144` | ⚠⚠ **the DEBRIEF — after a mission the player may optionally ask the crew what they thought** (owner, 2026-08-26).  ⚠ **PULL where `@X129`'s remarks are PUSH**: a nudge you can ignore and a question you can ask, neither of which is a wall.  ⚠⚠ **It is HOW `@X117`'s dominant axis accumulates**, which § P1 never said — *play again and notice* is a weak answer for the axis the design rests on, and this is where a sortie becomes a lesson, diegetically and optionally.  ⚠ Lives at the station (`DESIGN.md` § 16) | `PROGRESSION.md` § P2f |
| `@X145` | ⚠⚠ **the FENCE RELAXES on the INSTANCE and NOT on the RULES** (refined 2026-08-26) — `@X118` is strict in-mission because the instance is live and the player still has to decide; **once the base is finished there is no open question left to short-circuit**, so what happened may be said freely.  ⚠⚠ **But what to do DIFFERENTLY stays the player's to derive** — that is the transferable axis and it must be earned, so *"the tower was black from the third wave and nobody came"* passes while *"you should build tighter perimeters"* does not.  ⚠ `@X122`'s rules-vs-instance distinction, one layer up.  ⚠ The accounts stay PARTIAL and may disagree, so it does not become an oracle | `PROGRESSION.md` § P2f |
| `@X146` | ⚠⚠ **the roster is a SENSOR LOADOUT** — a sortie with no scout comes back with nobody who can say where the wave came from, so `@X125`'s template choice is **also a knowledge decision** and not only a labour one.  ⚠⚠ **And there is an empty chair**: a helper left behind is not there to be debriefed, so `DESIGN.md` § 14's *"cost of haste"* reaches into the station and takes an account away — `@X132`'s ledger producing consequence at zero design cost.  ⚠ Two refusals: **not a scoring screen** (§ 14 has no fail screen; a grade announces what the design says must be *felt across the sequence*) and **not an interrogation UI** (no dialogue tree; skippable in one action for `@X120`'s returning player) | `PROGRESSION.md` § P2f |
| `@X147` | ⚠⚠ **the crew are UNRELIABLE NARRATORS of the mission** (owner, 2026-08-26) — and it is what makes `@X145`'s relaxed fence safe: a debrief that relaxed the fence AND spoke with authority would hand over the dominant axis, so unreliability is the safety valve that forces the player to **triangulate**.  ⚠ Four sources, none of them lying: **position** (they saw their part), **expertise** (a miner reads a collapse as bad rock, a security officer reads it as a breach), **stake** (somebody left outside the wall remembers differently) and **salience** (what nearly killed them looms over what decided the base).  ⚠⚠ **It is LEARNABLE, which makes it a progression** — the crew's biases are stable RULES and the sortie is the instance (`@X122`), so calibrating your own people accumulates on `@X117` and nobody had to author it.  ⚠ Mechanically free: an account is limited to hexes the helper occupied, which the simulation already knows | `PROGRESSION.md` § P2f |
| `@X148` | ⚠⚠ **NOTHING THEY SAY IS FALSE — the skew is SELECTION, EMPHASIS and OMISSION** (owner, 2026-08-26: *"they will not state a wrong thing, they will just skew to their own perspective"*).  ⚠ *"Nobody came for that tower"* is TRUE; the skew is offering it as **the** reason the base fell when the wall was also mis-sited where the speaker never went.  ⚠⚠ **So the player can trust every sentence and still not have the picture** — a debrief can never push somebody toward a worse decision by asserting something false; the worst case is an INCOMPLETE picture, which is the intended difficulty.  ⚠ It also makes the build trivially safe: **a filter over a true event list**, weighted by domain and by where the speaker was — no fiction generator, nothing that invents a claim.  ⚠ Two supporting rules: bias misleads in **degree never direction** (or players tune the crew out and `@X137`'s onboarding role collapses), and bias is **consistent per character** (one that varies run to run is indistinguishable from lying and cannot be learned).  ⚠ This REFINES `@X129` rather than reversing it: **arbitrary error is noise, systematic bias is character** | `PROGRESSION.md` § P2c, § P2f |
| `@X149` | ⚠⚠ **they REPORT, they do not PRESCRIBE** — the debrief's version of *points, never concludes*.  A crew member may say what happened and offer an opinion built out of true things they witnessed; they may not recommend the fix.  ⚠ The owner's own statement of the goal is the test: *"the player need their own judgement to piece together what should be done/enhanced the next run"* — **a crew member who recommends the fix has done the piecing together**, and the player's progression is what paid for it | `PROGRESSION.md` § P2f |
| `@X150` | ⚠⚠ **THE CREW DO NOT KNOW MORE THAN THE PLAYER — they are on this planet for the first time too** (owner, 2026-08-26).  ⚠ Already the fiction: `SETTING.md` § The recruitment has Centaur Mining specialising in **automated asteroid mining** and taking a contract they have no experience of.  ⚠⚠ **The governing distinction is NOTICING vs KNOWING**: a past job gives them trained ATTENTION (a miner's eye goes to the ground) and never world knowledge (what Dryopea's ground actually does).  ⚠⚠ **So `@X129`'s fence needs no discipline to hold — they do not conclude because they CANNOT**, and a rule enforced by the world costs nothing to maintain where one enforced by restraint erodes the first time somebody writes a helpful line.  ⚠ They still onboard (`@X137`), by **drawing attention rather than explaining**: a lost player does not need an explanation, they need somewhere to look | `PROGRESSION.md` § P2c |
| `@X151` | ⚠⚠ **the crew LEARN ALONGSIDE the player, and it is audible progression** — arriving knowing nothing, they come to know what the player knows, so *"that ground looks soft to me"* becomes *"that's like the third base, the ground gave way there too"*.  ⚠ It is the crew's version of `@X117` and the one form of progression the player can HEAR.  ⚠⚠ **And it keeps them from ever overtaking the player**: they learn from the sorties the player ran, so their knowledge is a **subset** of what the player had access to — a crew member who knew something the player could not have learned is the failure `@X150` exists to prevent.  ⚠ Composes with `@X131`'s ledger (they may only refer to a base they were ON) and with `@X102`'s knowledge tree | `PROGRESSION.md` § P2c |
| `@X152` | ⚠⚠ **the crew are the WINDOW INTO THE BIGGER UNIVERSE** (owner, 2026-08-26) — *"the player gets pushed head-first into the scenario but the helpers have a background"*.  ⚠⚠ **It answers a hole nobody had named**: `SETTING.md` is ~2 300 lines and the game has **no delivery channel** for it — `@X137` refuses a tutorial, § HUD refuses an overlay, and `@X102`'s knowledge tree only covers what is found ON the planet.  ⚠⚠ **It does not contradict `@X150`**: the split is **Dryopea vs everywhere else** — novices about this planet, natives of the setting, so *the crew can explain where you all came from and not where you are*.  ⚠ It gives BACKGROUNDS a third job (starting skills, habits of attention, and now **a vantage on the universe**), which is why a hive dweller and a spacer are worth being different people | `PROGRESSION.md` § P2g |
| `@X153` | ⚠⚠ **FOUR channels, and they must not be collapsed into one dialogue system** — each has a different direction, trigger and cost: the **idle remark** (push, no task, what they notice here and now, free), **crew chatter** (push, ambient, overheard, the OUTER world, free), **ask them** (⚠ pull, in-mission, the player DRIVES to them and spends time — costing the clock and the position), and the **debrief** (pull, post-mission, their account of the sortie, free).  ⚠ *Ask them* passes `DESIGN.md` § What kind of game this is **outright**: it is a POSITION and not a keypress, the same shape as `plans/17` § T1's tower repair | `PROGRESSION.md` § P2g |
| `@X154` | ⚠⚠ **chatter YIELDS to the nudge** — `@X142`'s idle remark works because speech is RARE, so constant chatter turns a helper speaking into background noise and takes `@X137`'s onboarding role down with it.  ⚠ Chatter is sparse, must not repeat (a line heard twice is wallpaper), and while the player is stalled with something to point at, **the nudge gets the channel**.  Atmosphere is what plays when nothing needs saying | `PROGRESSION.md` § P2g |
| `@X155` | ⚠ **the crew carry truths 1 and 2; the PLANET keeps truth 3** — `SETTING.md` § The quarantine's three concentric truths divide cleanly: the public *haywire-AI* story and the military *nothing-leaves-without-a-permit* reality are the world the crew live in and may speak of (partially, with rumour attached), while the personality-altered AIs, the underground humans and the portal are found on the ground via `@X102` and are forbidden to the crew by `@X150`.  ⚠⚠ So the crew carry a great deal of setting **without touching the mystery** | `PROGRESSION.md` § P2g |
| `@X156` | ⚠⚠ **UI-LIGHT: presence IS the interaction, and there is NO LIST OF QUESTIONS ANYWHERE** (owner, 2026-08-26) — *"getting to them and interacting gives them a moment to comment but the player has no list of questions to ask of them"*.  ⚠ So *ask them* is not asking: it is **being there long enough that they say something**, which is `DESIGN.md` § 11 § Position triggers applied to conversation — *activated through motion, not by typing*.  ⚠⚠ **The player chooses WHO and WHEN; the crew choose WHAT** — which is `@X142`'s rule with the selector swapped, making push and pull **one mechanism with two doors**: the game picks the speaker when a helper is idle, the player picks by driving over, and the content is the speaker's either way.  ⚠ The line appears near them **in the world**, never in a panel; it is the same verb as repairing a tower.  ⚠ Applies at the station too (`@X146`) | `PROGRESSION.md` § P2g, § P2f |
| `@X157` | ⚠⚠ **the crew have real PERSONALITY — class decides what they notice, VOICE decides how they say it** (owner, 2026-08-26).  ⚠ A second axis: everything before it governs *what* is noticed (domain, attention, position), none of it governs **how they sound**.  A miner talks in effort, a scout in terse present-tense position reports, a hacker obliquely about how a thing is put together, a researcher hedged and unwilling to commit past the evidence.  ⚠⚠ **Personality reaches WORDING and EMPHASIS and never the FACTS** — `@X148` stays absolute, so a blunt person and a hedging one report the same thing and the player extracts the same information from either, just faster from one.  ⚠ The 2023 classes exist and carry a description; **a voice is the new authoring field** | `PROGRESSION.md` § P2h |
| `@X158` | ⚠⚠ **it is a FILTER, never a matrix — or the authoring cost explodes** — writing a line per (class × personality × situation) is combinatorial and kills the feature.  ⚠ Two stages instead: **domain picks a true thing to say, voice colours the phrasing**, so a new class costs its observations and a new voice costs a register — **additive, not multiplicative**.  Same shape as `@X148`'s filter over a true event list with one stage on the end.  ⚠ Two guards: a personality changes how **pleasant or quick** a source is and never **whether it is a source** (or `@X125`'s template choice stops being a decision), and clashing temperaments belong in **chatter and not in mechanics** — a compatibility table touching work efficiency is `@X131`'s affinity bar with extra steps.  ⚠⚠ **And personality is what makes the empty chair hurt**: you do not miss *a scout*, you miss the one who reported in three words | `PROGRESSION.md` § P2h |
| `@X159` | ⚠⚠ **the OFFICE — between missions the player may recruit, dismiss, or give a helper a task at the station** (owner, 2026-08-26).  ⚠ `DESIGN.md` § 9 only orders helpers **in-mission**; this is the half that never existed.  ⚠⚠ **The third option is the interesting one**: a helper who stays up runs the store, processes salvage, analyses AI cores or catalogues what came back — and **§ P2a's loop applies unchanged**, because a station task IS work, so it advances the skills it uses.  ⚠ **No "training" verb**: a training menu would be a shortcut around use-based advancement, which `@X124` exists to prevent.  ⚠⚠ **It turns the roster into an ALLOCATION problem every mission** — who goes down is labour and `@X146`'s sensor loadout, who stays up is paid work and **safety**, and the person left in the office is the account you will not have at the debrief | `PROGRESSION.md` § P2i |
| `@X160` | ⚠⚠ **dismissal is a real decision because the docs already gave it a dark ending** — firing somebody costs their practice (`@X124`) and their history (`@X131`), but the sharper consequence is `@X113`'s *"personnel can join competitors when not treated well"*: **a dismissed crew member can turn up on a competitor's payroll**, where neglect was the accidental route and dismissal is the deliberate one.  ⚠⚠ **The CHURN hazard** — fire the mediocre, hire fresh, repeat — is `@X125`'s reroll trap with dismissal as the button, and **§ P2a defends it structurally**: twenty sorties of practice beat any fresh template, so churning is bad play rather than a policed move.  ⚠ But that puts weight on *how fast the slow layer rises* — if statistics grow too slowly a veteran is not distinguishably better and the exploit opens after all | `PROGRESSION.md` § P2i |
| `@X161` | ⚠ **the office is a PLACE, not a roster screen** — `@X156` holds at the station too: you go to somebody and they say their piece, so **recruiting is going to the person you might hire and dismissing is going to the person you are letting go**, which is what a dismissal should cost.  ⚠ Fast to skip entirely, per `@X120`.  ⚠⚠ **And who is available is who is THERE** — never a refresh button and never a pool that re-rolls on re-entry (`@X125`); it should change for a **reason** (a competitor folded, a rescue brought somebody in), which makes it `@X122`'s *vary the instance* rather than a dice roll with a caption | `PROGRESSION.md` § P2i |
| `@X162` | ⚠⚠ **the office is also the MARKET — where salvage becomes money** (owner, 2026-08-26).  ⚠ It closes the contract's own loop: `SETTING.md` § The recruitment says **salvage IS the pay** and there was nowhere to be paid; it is the materials→points exchange `MATERIALS.md` § Open questions 1 recommended, and the 2023 knowledge tree already gates it (*"Economy — some items are valuable enough to sell to other star systems"*), so **knowing what is worth selling is DISCOVERED**.  ⚠ **REFINED** — the rule is *not a better way to play the SORTIE game*, aimed at a player who found a better income and stopped landing; **a player who chooses trading as their role outright is `@X165`**.  ⚠⚠ **Prices are STABLE and KNOWABLE**: one that fluctuates on entry is `@X161`'s refresh button on the economy, and waiting for a good day is the reroll trap again.  A price may move for a REASON.  ⚠ Stable prices are also what make in-mission salvage choices legible | `PROGRESSION.md` § P2i |
| `@X163` | ⚠⚠ **the market is where MULTIPLAYER finally has a PLACE** — `DESIGN.md` § 20 has carried multiplayer as an ambition with **no interface anywhere in the design**.  ⚠⚠ **A shared market needs none of the netcode a shared sortie would**: players never have to be in the same base at the same time, so it is **asynchronous** and fits § 14's bounded one-sitting sessions exactly — the office is what sits in the gaps.  ⚠ So the first multiplayer feature is not co-op but **two players who never meet, trading through a station**; the second is § 20's abandoned-base inheritance, the same asynchrony one layer out | `PROGRESSION.md` § P2i |
| `@X164` | ⚠⚠ **KNOWLEDGE IS NOT TRADEABLE, and it is a hard line** — materials, salvage, equipment and tower-tops may cross between players; knowledge may not.  `@X117` makes what the player knows the DOMINANT progression and `@X118` forbids buying answers, so a player who could purchase *"a gate buys nothing"* would be buying the axis the design rests on.  ⚠ **The same applies to NPC vendors**, and it is the office's version of `@X155`: **the office carries the OFFICIAL world and the planet carries the real one** — a trader may quote the price of lithium, nobody at the station can tell you what is under the buried city.  ⚠ **Crew are an open maybe**: `@X160` already sends a dismissed helper to a competitor, so a helper changing employer between players is that event with a name on it | `PROGRESSION.md` § P2i |
| `@X165` | ⚠⚠ **a pure TRADER player is legitimate, and the role has to be POLITICAL** (owner, 2026-08-26: *"not against a pure trader player, but that has to become a political role in station too.  And it should not be a spreadsheet role"*).  ⚠ The exploit and the role differ in what the game is MADE of — margins versus politics.  ⚠⚠ **`@X162`'s stable prices are what FORCE it to be political**: with no arbitrage, no market timing and nothing to buy low and sell high, **the only lever left is WHO you deal with** — so a rule written against the reroll trap is what makes *not a spreadsheet role* achievable rather than aspirational.  ⚠ The material is already in `SETTING.md`: permits (*"permit on file = pass; permit missing = destroyed"*), competitors approved and not, the cordon's own officials, nine factions with `economy` among them, and the armada that comes *"when somehow forced to"* | `PROGRESSION.md` § P2i |
| `@X166` | ⚠⚠ **the trader CANNOT hollow out the game, because it feeds on it** — salvage is produced by sorties and by nothing else, so **a trader with no sortie-runners has nothing to trade**.  That is structural rather than a rule.  ⚠ A trader's scarce resource is not money but **whose side they are on**, spent once per decision in front of people who remember — recognisably `@X131`'s ledger one layer out.  ⚠ They may broker **contracts, access, supply and permits**; they may **not** broker knowledge (`@X164`), which is the natural trader fantasy and exactly the forbidden one: brokering *who lands where* is fine, brokering *what they will find* is selling the dominant axis | `PROGRESSION.md` § P2i |
| `@X167` | ⚠⚠ **RECORDED HONESTLY: the trader is the one part of this design that is NOT a tower defence** — `DESIGN.md` § And the DEEP layers asks whether a thing resolves into position, terrain or timing, and a trader's game resolves into **relationships and consequences**.  It does not pass and is not meant to: it is a different ROLE, not a mechanic inside the tower defence.  ⚠⚠ **The boundary that matters: the trader role must not change the tower-defence game for the people playing it** — they may decide who gets supplied, never gain a lever that reaches into somebody else's base, alters their waves or plays their sortie.  ⚠ Written down rather than smoothed over, because the next contributor will otherwise delete it for failing the genre test or let it grow into the main game.  ⚠ It is naturally a **multiplayer** role: single player would need NPC operators generating real supply and demand, which is the economic simulation `ROBOT_ECONOMY.md` already refuses | `PROGRESSION.md` § P2i |
| `@X168` | ⚠⚠ **THE CREW SECTION — another player may pick up who you left, and they may not come back** (owner, 2026-08-26).  ⚠ Half of it is already in `DESIGN.md` § 9 § Stranded helpers (*"or (multiplayer) for a different player who lands nearby"*); what is new is **what happens after the pickup** — the ledger cashed in, so a relation that is *frankly bad* lets them stay with their rescuer.  ⚠⚠ **It answers `@X164`'s open question and better than trading would: crew move between players, but never as GOODS** — not a transaction but a **choice**, through abandonment and rescue rather than the market.  **You cannot buy somebody's crew; you can only be the one who came for them.**  ⚠ And it needs **no new mechanic** — `plans/14`'s retrieval path is shipped, so a cross-player rescue is that path with a different name on the record | `PROGRESSION.md` § P2e |
| `@X169` | ⚠⚠ **the default must be RETURN, or multiplayer becomes a punishment** — if defection were common, leaving anybody behind would read as being robbed for playing multiplayer.  The owner's calibration is the word *frankly*: the ordinary case is **a courtesy**, and defection is rare and EARNED out of a relation that was already bad.  ⚠⚠ **Which makes returning a helper a favour with NO mechanical reward** — so a player who reliably brings people home acquires a **reputation**, and reputation is `@X165`'s political currency arriving on a second channel for free.  ⚠ The choice reads out of the LEDGER and stays legible (`@X131` forbids a bar): *"you left me twice, he came for me"*, never *loyalty 34%*, and never a dice roll at the moment of rescue | `PROGRESSION.md` § P2e |
| `@X170` | ⚠ **what crosses is a PERSON, not an ANSWER — so `@X164` survives intact.**  A defector carries practice (`@X124`) and history (`@X131`) and what they learned alongside their old employer (`@X151`), which looks like forbidden knowledge transfer and is not: they **report without prescribing** (`@X149`) and **point without concluding** (`@X129`), so the new employer gains **a colleague with a history elsewhere** rather than a rule — *"that's like a base I was on before"*, about a base the new player never saw.  ⚠⚠ **It degrades to single player with no second design**: without other players the rescuer is a competitor, which is `@X160` already — one mechanic, two audiences.  ⚠ And it is a **second asynchronous channel** (`@X163`): the two players never have to meet | `PROGRESSION.md` § P2e |
| `@X171` | ⚠⚠ **being left behind is NOT necessarily grim — a helper may build a life** (owner, 2026-08-26): shelter and a vegetable garden out of what the wildlife killed, filters for the air, waiting the robots out.  ⚠ The fiction was ready — `SETTING.md` § Nobody is attacking anybody means **a lone human with no jammer is not a target**, § History gives the natives' air filters, `@X100`'s pollen litters the map with machines nobody is coming for, and § The settlers today have run this playbook for generations.  ⚠⚠ **How well they do is a READOUT OF THE CREW YOU BUILT** — `scrounge`, `build`, `mine`, `stamina`, every one raised by work the player gave them (`@X124`), so the homestead is the practice loop's own verdict delivered back as a place.  ⚠⚠ **It changes the TONE and not the COST**: the player still loses their labour, their debrief account and their practice — what changes is the guilt, which turns going back from a duty into a want.  ⚠ And it opens a THIRD ending beside return and defection: **they stay**, which the 2023 notes already anticipate (*"most settlers don't want to leave"*) | `PROGRESSION.md` § P2e |
| `@X172` | ⚠⚠ **hunting for other players' stranded helpers is CONTENT, not economy** — a third asynchronous channel after the market and the rescue, and the first that is not trade.  ⚠⚠ **Its point is the last clause of the owner's idea: *let them see what is possible*** — a homestead built out of parts in a place the finder thought unsurvivable **TEACHES**, and it teaches the only way `@X137`'s no-tutorial ruling permits.  ⚠ Authored by play rather than by hand, which makes it the cheapest content in the design and the only kind that could not have been written in advance | `PROGRESSION.md` § P2e |
| `@X173` | ⚠⚠ **player-built CACHES — abandonment as a PLAN** (owner, 2026-08-26).  A rocket has limited weight and volume, so there is always more on the ground than will lift: bury the overflow as a **kickstart for a later mission near the same place**.  ⚠ It **sharpens** the scramble rather than easing it — the manifest becomes *what do I take, what do I hide, can I get back to it*.  ⚠⚠ **Other players may find one and crack it open, "not easy"** — PvP with no combat and no netcode.  ⚠ Three rules against griefing: hard to find, hard to crack, and ⚠⚠ **cracking should be LOUD**, a lockpick that draws the planet's attention, which makes robbing somebody a decision rather than a chore.  ⚠ Ownership stays legible, because it feeds `@X169`'s reputation | `DESIGN.md` § 20 |
| `@X174` | ⚠⚠ **THE PLANET ACCUMULATES OTHER PEOPLE'S PLAY** — an abandoned base, a cache (`@X173`) and a stranded helper's homestead (`@X171`) are **one shape**: things a player leaves behind that persist, that another player finds, and that nobody authored.  ⚠ Each is a consequence made physical, and together they are the **cheapest content in the design** — generated by people playing, impossible to write in advance, and ⚠⚠ **it teaches**, which is the only kind of lesson § There is NO TUTORIAL permits.  ⚠ All three want per-planet persistence and none wants netcode beyond a shared store | `DESIGN.md` § 20 |
| `@X175` | ⚠⚠ **TOWERS ARE ALWAYS STRONGER THAN A PLAYER — so a base is IMPREGNABLE TO A PERSON** (owner, 2026-08-26).  ⚠ Not a new rule: `DESIGN.md` § 8 already makes the player a noncombatant with no weapon and § 7 makes towers the things that fight — this is that, applied to two players.  ⚠⚠ Therefore **PvP cannot be raiding-by-violence**: the only way into somebody's base is **invitation** (friendly towers), and the only way against them is **the planet** | `DESIGN.md` § 20 |
| `@X176` | ⚠⚠ **the aggressive route runs through the ROBOT ECONOMY, not through combat** — *"unless you can hack enough robots to overwhelm another player"*.  You do not attack a player, **you point the planet at them**, which preserves the genre rather than bending it: the enemy is still robots, the defence is still towers and walls.  ⚠ Every piece exists — § 9's hacking helpers, `MATERIALS.md`'s AI core (takeable, never manufacturable), the `hack` skill's levels, `ROBOT_ECONOMY.md`'s routes.  ⚠⚠ **Deliberately EXPENSIVE**: *enough* robots is a campaign-scale investment, so hostility is decided over sorties and never launched on a whim — `SETTING.md` § Nobody is attacking anybody holding at the multiplayer layer.  ⚠⚠ **And the attack arrives as CONTENT, not griefing**: the defender's answer is towers, walls, position and timing, so nothing arrives they have no vocabulary for | `DESIGN.md` § 20 |
| `@X177` | ⚠⚠ **the DEFAULT PvP is a RACE, not a fight** — *"other than gathering resources quicker than they are"* — two operators working the same region, contesting **the environment around the bases**, which is where this game already spends its time.  ⚠⚠ **And it SETTLES `ROBOT_ECONOMY.md` § Open questions 1: per-PLANET**, because you cannot compete for a mine that only exists inside your own instance.  That question's own recommendation (author per-map, keep node identifiers global) is the migration path | `DESIGN.md` § 20, `ROBOT_ECONOMY.md` |
| `@X178` | ⚠⚠ **co-op is FREE, because every action is position-triggered** — *"unless they set their towers to friendly for you"* is a permission, and once given the feature is already built: § 11 § Position triggers makes every action a place rather than a keypress, and **none of them asks who you are**.  A guest repairs towers (20 s of standing there), clears bodies (`salvage_at` takes a hex) and delivers salvage (driving over it).  ⚠⚠ **So letting somebody in IS the co-op feature** — no shared-control model, no party system, no second input path.  ⚠ Permission is a **TOWER STATE**, which § HUD already speaks as a colour, so it is diegetic and legible across the base — and revocable, which makes **betrayal possible** and the trust worth something.  ⚠ Stays asynchronous-capable: an attack on an offline player is the abandoned-base bullet one step over | `DESIGN.md` § 20 |
| `@X179` | ⚠⚠ **TOWER HACKING is scoped to 2v2 / 3v3 MATCHES, where whole bases can be taken over** (owner, 2026-08-26).  ⚠⚠ It is **the one thing that would break `@X175`** — turning a tower makes the base's own strength the attacker, and being killed by your own perimeter is not a wave anybody can answer with towers and walls.  ⚠⚠ **The team scoping is what makes it safe, and the reason is CONSENT plus a COUNTER**: in a match both teams signed up and a **teammate can drive to a hacked tower**, where in the open world a campaign base is at stake and possibly nobody can answer.  ⚠ *Open-world PvP is something that happens to you; a match is something you entered.*  ⚠⚠ **And a takeover is a contest of PRESENCE** — § 7's repair is 20 s of standing there, so **hacking and repairing are the same verb pointed in opposite directions**, and § 8's noncombatant player survives: you take a base by BEING at its towers, never by shooting | `DESIGN.md` § 20 |
| `@X180` | ⚠ **the first SYNCHRONOUS feature in the design** — the market, the crew rescue, caches and even an attack on an offline player are all asynchronous, but **a base takeover needs both sides present**, so it wants real netcode rather than a shared store, plus a match lifecycle, teams and a win condition.  ⚠ It still fits § 14's **bounded one-sitting** shape (a match is exactly that) but not its solo structure, and small teams are what keep both the netcode and the base scale legible | `DESIGN.md` § 20 |
| `@X181` | ⚠⚠ **it must not leak into the campaign world** — a match played over a campaign base would delete `@X175`'s impregnability by the back door.  *Recommendation: **matches are their own container*** — bring a crew and a loadout, risk neither the base you have been building nor the people: **match casualties do not carry**, or a lost match costs a crew member with a campaign's practice and history on them (`@X131`), which is a stake nobody consented to when they queued.  ⚠ Tower hacking stays a **MATCH verb** until somebody argues it into the persistent world, and that case must answer **what a solo player does when their perimeter turns on them with no teammate to send** | `DESIGN.md` § 20 |
| `@X182` | ⚠⚠ **NETCODE IS GATED ON DEMAND, and the competitive mode is SEPARATE from the campaign** (owner, 2026-08-26).  ⚠⚠ It draws a clean line — **asynchronous vs synchronous**: the market, crew rescue, caches, homesteads and attacks on offline bases all ship on **persistence alone** (a shared store), while only team matches and base takeover need netcode.  ⚠⚠ **So netcode is not on the critical path at all**, and the roadmap carries five multiplayer features with no networking phase in front of them.  ⚠ **And the deferral PROTECTS the campaign design**: building netcode early would bias every decision toward synchronicity — bases designed for matches, waves balanced for teams — where deferring keeps the campaign primary and makes the match mode INHERIT from it | `DESIGN.md` § 20 |
| `@X183` | ⚠ **the trigger is a shipped game with players asking for COMPETITIVE play specifically** — not co-op, and not *"multiplayer"* in general, because the asynchronous half already is multiplayer.  ⚠⚠ **And the trigger may never fire**: trading through a station, racing for a mine, cracking a cache and taking somebody's abandoned crew are **already non-cooperative**, so the cheap features may absorb the demand — which would be a good outcome rather than a disappointing one.  ⚠⚠ **Do not be fooled by loft shipping netcode**: that makes the TRANSPORT cheap, while the expensive part is the DESIGN — match lifecycle, teams, matchmaking, win conditions, balance and anti-grief, none of which the campaign needs and all of which would be maintained for ever | `DESIGN.md` § 20 |
| `@X184` | ⚠⚠ **a TERRITORIAL mode — hackable installations, contested, both sides building defences** (owner, 2026-08-26).  ⚠⚠ **It is a REARRANGEMENT rather than a new game**: `ROBOT_ECONOMY.md`'s six installation types are the neutral structures, `@X176`'s hacking is the way to take them, and towers and walls are the defences — the core game unchanged.  ⚠⚠ **It collides with `ROBOT_ECONOMY.md` § No underground level, and the container is the resolution**: that refusal was made for the CAMPAIGN and still stands there, while a separate mode may afford a second movement model precisely because it does not serve the campaign — the first real payoff of `@X181`/`@X182`'s separation.  ⚠ Cheaper version worth trying first: **entrances on the surface map**, territorial without descending | `DESIGN.md` § 20, `ROBOT_ECONOMY.md` |
| `@X185` | ⚠⚠ **THE BALANCE RULE: a big area is much harder to guard, so the leader is automatically more vulnerable to PRECISE attacks** — a catch-up mechanic **with no rubber band**, because nobody is given anything: the leader's advantage IS their weakness, by geometry.  ⚠⚠ **And dryopea has already measured it** — `@M020`'s siege front is the wall's WIDTH, `plans/12` § B7's sealed-vs-gated clock, `plans/17` § T3's shuttling-vs-parked upkeep, and `PROGRESSION.md` § P7's compact-vs-spread tension.  ⚠⚠ **The geometry enforces it because installations CANNOT BE MOVED** — a mine is where the ore is, so holdings are inherently spread and there is no consolidating move; the leader must **choose** what to defend.  ⚠ *"Precise"* is load-bearing: the trailing player needs **better targeting, not more force**, which puts `@X117`'s dominant axis in charge of a competitive match | `DESIGN.md` § 20 |
| `@X186` | ⚠ **two things to tune rather than assume** in the territorial mode — **expansion must be worth the exposure** (if defending at scale is impossible, nobody expands and there is no equilibrium), and **precision must cost too** (or the trailing player harasses for ever; `@X176`'s *hacking is slow and expensive* is what makes a precise attack a spent investment rather than a free poke) | `DESIGN.md` § 20 |
| `@X187` | ⚠⚠ **the multiplayer block is a DESTINATION, not a QUEUE, and its dependency is an AUDIENCE rather than a technology** (owner, 2026-08-26: *"it needs an active community of people that only the base game can provide"*).  ⚠⚠ **Every mode is cheap BECAUSE it reuses the base game's assets and mechanics — and that reuse only works if the base game is finished and coherent first.**  ⚠ So the length of `DESIGN.md` § 20 is **not a signal of its priority**: it accumulated in one sitting because the ideas compose, and `plans/ROADMAP.md` § The critical path is unchanged with **BUILDING still the biggest missing mechanic**.  ⚠⚠ **What serves these modes today is building the base game well** — a mechanic bent early to suit a mode with no players yet is the way to lose both | `DESIGN.md` § 20, `plans/ROADMAP.md` |
| `@X188` | ⚠⚠ **building out the CAMPAIGN brings multiplayer much closer — not by attracting players, but because it builds the PIECES** (owner, 2026-08-26).  ⚠ Each for its own reason: **per-planet persistence** (Tier D) is the **sole prerequisite of five features**; the **robot economy** (Tier E) is the territorial mode's neutral structures; **hacking** (Tier C) is `@X176`'s whole aggressive route; the **crew layer** is cross-player rescue and defection entire; the **station office** is the market and the trader.  ⚠⚠ **So the distance shrinks as a SIDE EFFECT** — the co-op permission (a tower colour) is close to the only new part, because § 11's position triggers already make every action ownership-agnostic.  ⚠⚠ **The one actionable rule: do not gold-plate a campaign piece for multiplayer, and do not design one that FORECLOSES it** — keep persistence per-planet-per-player rather than per-session, and keep code asking *which* player rather than assuming *the* player (`salvage_at` takes a hex and `blocker_at` answers which vehicle, so today's is healthy) | `DESIGN.md` § 20 |
| `@X189` | ⚠ **the advancement RATE: bounded per session, and slower where a skill is already deep** (owner, 2026-08-26) — *"they still progress but probably only after multiple sessions"*.  ⚠ **Two rules, two problems**: the per-session bound stops one long base transforming somebody and stops grinding inside a run; the depth curve stops a veteran running away.  ⚠ The second is half-designed already — `@X124`'s fast skills / slow statistics is the same instinct applied to the LAYER, and this adds the curve WITHIN each.  ⚠ The first composes with `@X126`: a session's gain is bounded **and** conditional on boarding.  ⚠ A shape worth considering — let `@X113`'s **endurance pools** do the per-session bounding, so the limit EMERGES (a tired person works less, so learns less) rather than being a hard cap with a cliff that makes short sessions efficient | `PROGRESSION.md` § P2a2 |
| `@X190` | ⚠⚠ **OPEN — diminishing returns and the anti-churn defence pull OPPOSITE WAYS.**  `@X160` defends fire-the-mediocre-hire-fresh *structurally*: twenty sorties of practice beat any fresh template.  ⚠⚠ **A depth curve weakens exactly that** — a fresh hire climbs the steep part fast while a veteran crawls, so if the curve is steep enough a replacement reaches *good enough* in a few sessions and churning is viable again: `@X125`'s reroll trap arriving through the maths rather than through a button.  ⚠⚠ **The discriminator is whether there is a CEILING and how close ordinary play gets to it** — asymptotic to a rarely-approached high value keeps the gap wide and both rules hold; convergent to a reachable ceiling makes the veteran's edge temporary and reopens churn.  ⚠ Same number `PROGRESSION.md` § Open questions 6 already flags as needing a measurement rather than an argument, now with a second constraint on it | `PROGRESSION.md` § P2a2 |

### sim — cost and scale ([`plans/22`](../plans/22-the-field-cache/README.md))

| code | decision | owner |
|---|---|---|
| `@X030` | the field is **cached by EPOCH**, and the epoch is bumped **inside** the two mutators (`height_raise` / `height_clear`) — never at a call site | `22-S1` |
| `@X031` | the sweep is **bounded by the ROSTER**, by **PATH** distance never straight-line — the bubble is a sphere and a route can be far longer | `22-S3` |
| `@X032` | **invalidate on EFFECT, not on EVENT** — the field changes only if an edge flipped | `22-S4` |
| `@X033` | ⚠⚠ **simulation granularity must NOT follow the CAMERA.**  If it does, where the player looks changes the outcome.  The boundary is the interaction radii | `plans/22` § What this plan does NOT build |
| `@X034` | **incremental field repair stays deferred** — an incrementally wrong field routes enemies through a wall.  ⚠ Its equality gate is already written and green | `22-S5`, `11-F8` |

### method

| code | decision | owner |
|---|---|---|
| `@X035` | **one caller of `wave_tick`** — asked by COUNT (`play_ticks`) or DURATION (`play_advance`), never one folded into the other | `19-P1`, `@M003` |
| `@X036` | the **mode gates the CLOCK, never the seam** — `in_playing` says what keys mean, `PlayState.playing` says whether time reaches the sim | `19-P3` |
| `@X037` | **probes are kept when their answer is load-bearing** — [`probe/`](../probe/README.md), with their own `loft.toml` so an experiment does not become a dependency | `probe/README.md` |

---

## Measurements

⚠⚠ **A measurement AGES, and the stale one gets quoted** — `CLAUDE.md`
§ Profiling records that *"58% is `canvas()`"* was cited three plans after it
stopped being true.  **Every row here carries a date.  Quote the code, not the
number, and re-measure before optimising.**

| code | measurement | date | source |
|---|---|---|---|
| `@M001` | **`flow_sweep` and the lookups under it are ~75%** of the suite's interpreted time; `classify_canvas` is 7.5% | 2026-08-15 | [`PROFILING.md`](PROFILING.md) |
| `@M002` | a GL frame captured under `xvfb` and decoded classifies with **`other` = 0** over 76 800 px — zero colour drift | 2026-08-15 | [`probe/r0`](../probe/r0/), `RENDERER.md` § R0 |
| `@M003` | `n × TICK_SECONDS` through an accumulator is **one tick short for 602 of the first 1000 `n`** — and an exact product does not save it (`n = 12` gives exactly 8.0 and answers 11) | 2026-08-15 | `19-P1` |
| `@M004` | a **sealed** wall nearly **doubles** the fall clock; a wall with a **GATE buys nothing**; a tower is **+16 ticks** since the pre-walk window (69 / 112 / 128) | 2026-08-15 | `12-B7`, `16-W2` |
| `@M005` | the authored **seven-wave list plays FOUR** and falls at **320** with every tower black — a tower is 300 HP of ammunition against a 6 150 HP list.  ⚠ It was **321** from plan 16 W4 until plan 24 W1 took one tick off it (`@M020`); prose quoting 321 is quoting the older reading | 2026-08-17 | `16-W4`, `24-W1` |
| `@M006` | the same retrieval is worth **one tick** when the job is gone, and **+76 points** when it is not | 2026-08-14 / -15 | `16-W4`, `17-T3` |
| `@M007` | seven towers and **two SHUTTLING helpers clear all 205 robots**; the same two **parked** reach 5/7 and the base falls | 2026-08-15 | `17-T3` |
| `@M008` | binding a `FlowField` to a local **copies the heap value** — **2250×** the cost of reading it in place, live for four phases across 490 green tests | 2026-08-13 | `11-F8` |
| `@M009` | the crew-size spread is **123 / 135 / 138 ticks** (was 77 / 214 / 242 before the pre-walk window) — a roster buys COVERAGE, not throughput | 2026-08-15 | `14-H2`, `16-W2` |
| `@M010` | a **mirrored base is not a symmetric one** — 112 vs 211 ticks on a map that looks mirror-symmetric, until both walls were extended past the walkable band (214 vs 211) | 2026-08-14 | `14-H2` |
| `@M011` | four of a class into a sealed band breach at **23 / 35 / 50 / 96 / 454 ticks** (miner / builder / robot / harvester / scout) — **20× between the two ends**, from a mover that cannot tell them apart.  ⚠ Read at K0 as 20 / 35 / 50 / 96 / 456; the miner's and the scout's moved in `23-K2b` and **not because a rate changed** — a breach clock counts from placement, so it contains the WALK, and those are the two classes whose speed moved.  The three unmoved rows are what say the rates are untouched | 2026-08-15 (re-measured `23-K2b`) | `23-K0`, `23-K2b` |
| `@M012` | ⚠ the scout's first rate was **wrong by measurement, not by arithmetic**: 0.2 HP/s breached at **231** ticks, inside the 321 a real base lasts (`@M005`), because the sum had been done against the 100 HP BRACED figure while the siege chews the 30 HP END | 2026-08-15 | `23-K0`, `12-B3` |
| `@M013` | ⚠⚠ **1.5 hex/s is a speed at which the rounding guard CANNOT FIRE.**  Over sixty ticks of the 1/1.5 s timestep the epsilon is worth a whole hex at **1.0, 1.2, 1.8, 2.0 and 2.5** hex/s and worth **nothing** at 0.5, 0.75, 1.5, 2.25 and 3.0 — and it is worth a hex to a TENTH-length tick too.  So a class K2b gives 1.0 hex/s would lose a hex every forty | 2026-08-15 | `23-K2a`, `17-T1` |
| `@M014` | ⚠ **the guard's invisibility, measured rather than argued**: with `ENEMY_PROGRESS_EPSILON` set to 0.0, all **1128 pre-K2a tests and all 569 measurements stay green** and only the three assertions written to look for it fail.  This is what *an epsilon whose removal leaves the suite green* looks like when you check | 2026-08-15 | `23-K2a`, `17-T1` |
| `@M015` | **banking at 1× is the identity, bit for bit** — the mover was rebuilt on `speed × tick_seconds` with every number held, and the corpus did not move: **1128 → 1138 tests** (the ten new ones are K2a's own) and **30 scripts / 569 measurements unchanged** | 2026-08-15 | `23-K2a` |
| `@M016` | **the three arrival clocks**: nine hexes of identical corridor take a scout **6** ticks, a robot **9** and a miner **14** — 2.5 / 1.5 / 1.0 hex/s.  ⚠ A miner stands STILL on one tick in three and a scout takes TWO hexes on two in three, which is what a rate looks like once a tick is no longer a hex (`@X058`) | 2026-08-15 | `23-K2b` |
| `@M018` | ⚠⚠ **a wave is as dangerous as its FASTEST class, and no more.**  Twelve robots into one sealed band: **12 miner falls at 94**, *4 builder + 8 miner* at **104** (12 builder = 100), *4 robot + 8 miner* at **119** (12 robot = 115), *4 harvester + 8 miner* at **164** (12 harvester = 161), *4 scout + 8 miner* **never** (12 scout = never).  Every mix lands within **four ticks of a PURE wave of its front class** and none of them near the eight miners that are two thirds of it.  ⚠ The direction inverts the intuition — the hardest-biting class in the game, added BEHIND a weaker one, makes the wave *slower*.  ⚠ Cause: only **three** hexes of the wall are ever attacked (the approach fan's width, not the wall's — a seven-row wall does not add a fourth), an enemy blocked by a COMPANION attacks nothing (`11-F7b`), and since `23-K2b` the fastest class arrives first — so four robots own the whole front.  ⚠ It is a CLIFF: the first scout swapped into twelve miners costs the wave nothing at all, the fourth costs it the base.  ⚠ **The ROSTER order is worth nothing** — scouts first, scouts last and scouts alternated all land on the same tick, so `23-K0`'s *order is worth 20x* is now a statement about POSITION.  The fix is `@X064`.  ⚠⚠ **SUPERSEDED by `@M020`** (plan 24 W1, 2026-08-17): the cause was not the fan's width but the desire field's shape (`@M019`), and one precedence retired the headline — *4 scout + 8 miner* went from *never* to **126** | 2026-08-15 | `23-K3`, `12-B3`, `11-F7b`, `23-K2b` |
| `@M020` | ⚠⚠ **what *arriving beats queueing* is WORTH — `@M018` retired.**  Plan 24 W1's one precedence, and the same five waves of twelve: **12 miner 94** (unmoved), *4 builder + 8 miner* **104 → 101**, *4 robot* **119 → 116**, *4 harvester* **164 → 122**, *4 scout* ***never* → 126**.  ⚠ The front went **3 → 4** hexes on a five-row wall and **3 → 6** on a seven-row one, so it is the WALL's width now rather than the approach fan's.  ⚠⚠ **The new rule: a wave is worth its front class PLUS whatever the front class cannot COVER** — four screens against a five-hex face leak exactly ONE miner, and a soft screen loses 39 ticks to it while a hard one loses nothing (the builder row is the negative control).  ⚠ The cliff is still a CLIFF — the first three scouts are worth nothing and the fourth thirty-two ticks — but it no longer buys immunity.  ⚠ **The ROSTER order is still worth nothing and the test got STRONGER**: it used to compare three bases that never fell, and three zeroes are equal for any reason at all; all three now fall on the same tick.  ⚠ Corpus cost: **16 measurements moved**, `@M005` 321 → **320**, and a wider front makes most bases LAST LONGER (`a-base-on-two-fronts` 123 → 132) because a besieger that stops at the wall is not walking on to drain the wallet | 2026-08-17 | `24-W1`, `24-W2`, `@M018`, `@M019` |
| `@M019` | ⚠⚠ **the three-hex front is the DESIRE FIELD's shape, and the rule five documents name is one dryopea already has.**  K3's band, core at `(0,0)`, wall across `q = 6`: the face column `q = 7` reads **8 / 8 / 7 / 8 / 8** — a ring around the core has ONE minimum on a straight face, so only the minimum and the two hexes where the lateral step runs out have no legal closer step.  ⚠ That is **three for any wall length** — `(7,±3)` steps to `(7,±2)` exactly as `(7,±1)` steps to `(7,0)` — which is why a seven-row wall does not add a fourth.  ⚠⚠ **All five face hexes TOUCH the wall and two of them walk away from it**: an enemy attacks only when it cannot walk (`enemy_target`: *"only when NOTHING down it is legal"*), and the equal-distance sidestep offers `(7,-1)` a choice between `(7,-2)` along the face and `(8,0)` **back off it**.  ⚠ So `@X064`'s *equal-distance sidestep* is misnamed: the missing rule is a PRECEDENCE — *arriving beats queueing* — and it must land in `enemy_walk_desire` AND `enemy_target` or they disagree.  ⚠ `enemy_target` is occupancy-blind by SIGNATURE, so the rule has to be phrased *"I am beside an attackable wall"* rather than *"my closer steps are held"* | 2026-08-17 | `24-W0`, `@M018`, `23-K3`, `12-B3`, `11-F7b` |
| `@M017` | ⚠⚠ **the guard `@M014` measured as unreachable now FIRES, on BOTH gates** — with `ENEMY_PROGRESS_EPSILON` at 0.0: **7 of 1149 tests fail** and `scripts/validate.sh` goes RED (581 measurements, not 597), where the same experiment at K2a left 1128 tests and 569 measurements entirely green.  ⚠ Only **3** of the 7 are the assertions written to look for it; the other four are ordinary behaviour — a scenario round trip, a per-enemy-rate gap, a bank total and the `.keys` file itself.  ⚠ It takes **three ticks**, not sixty: 3 × 2.5 hex/s over a 1/1.5 s tick sums to **4.999999999999999**, so the fifth hex exists only because the guard releases it.  The lesson is `@X063` — an invisible guard is a defect waiting for whoever changes the number, and the cheapest fix is to ship a value that can see it | 2026-08-15 | `23-K2b`, `23-K2a`, `17-T1` |

| `@M021` | ⚠⚠ **the world frame is measured, not argued — and a handedness flip is a MIRROR that no camera setting undoes.**  Eight azimuths at elevation 89°, asked whether east lands screen-RIGHT and north screen-UP with neither tilted: the **+y-NORTH** frame answers yes at exactly **one** of the eight (270°, which is why `@X066` pins it) and the **+y-SOUTH** frame at **none** — it gets east-right at 270° or north-up at 90° and never both.  ⚠ Supporting number for *why 89° and not 90°*: two opposite azimuths move the eye **2.79 m** apart at 89° and **2.6e-13 m** at 90°, so at vertical the azimuth is not clamped, it is DEAD — the screen's roll becomes floating-point residue.  ⚠ 90° does **not** collapse the view the way the design predicted (`cos(π/2)` is 6.1e-17, not 0), so every valve reads healthy | 2026-08-17 | `21-R1`, `@X065`, `@X066` |
| `@M022` | ⚠⚠ **`camera_overview` at 89° IS the editor's view, to a tenth of a degree.**  Twelve hexes on a ring of radius ~10 put through the game camera and through `render.loft::world_to_canvas` on the editor's own 960×720 at `VIEW_PPM`: worst bearing disagreement **0.0014 rad** (0.08°) and the distance ratio uniform at **3.070 – 3.087** (0.56%) — a similarity, not merely "both top-down".  So `@X009`'s one-camera-two-presets holds and `@X010`'s eventual collapse of the software rasteriser is a migration.  ⚠ moros's follow formula ported verbatim puts the eye **exactly abeam** at all four cardinal bearings — along-track component 0.0, not negative — so it tracks, eases and looks like a working camera (`@X067`).  ⚠⚠ **This gate read a PERFECT 0.0 rad twice while measuring nothing** — once on [loft#955](https://github.com/loft-lang/loft/issues/955)'s empty const (twelve null hexes, all at the screen centre) and once comparing bearings in NDC, where the aspect ratio is baked in.  *An exact zero out of an integer-pixel-versus-float comparison is a tell, not a result* | 2026-08-17 | `21-R1`, `@X065`, `@X067` |
| `@M023` | ⚠⚠ **the ease is frame-rate independent to the BIT, and moros's linear form is not.**  Easing 0 → 1 over one second: exponential reads **0.9975212478233336** as one frame of 1 s *and* as sixty of 1/60 s; the linear form reads **1.0** against **0.9982029897000857**.  ⚠ The negative control is in the same test — the linear pair is measured beside it and asserted to DISAGREE, so a stubbed ease cannot pass.  ⚠⚠ **And what the ease BUYS on a lattice**: over 240 frames with the vehicle stepping a hex every twentieth, the un-eased follow camera moves on **12 of 240** frames with a worst frame of **1.299 m**; the eased one moves on **221 of 240** with a worst frame of **0.143 m** — nine times smaller, eighteen times more often.  That is `@X069`'s whole argument as a number | 2026-08-17 | `21-R2`, `@X068`, `@X069`, `19-P0` |
| `@M024` | ⚠ **what shortens the boom, and by how much.**  Resting boom **5.831 m** (the design's 3 m up / 5 m back).  A `wall_high` across the whole sight line lends **0.708 m**; one `wall_high` at the far cell (3 of 4, under a ray at **3.10 m**) lends **3.624 m**; a `wall` at that same cell lends the **whole** boom, because 3 m is under the ray and 5 m is not — so the camera reads a HEIGHT, never a kind, exactly as `12-B5b` measured for a shot.  ⚠ Near the vehicle the two kinds are indistinguishable: one hex out the ray is only 1.6 m up and both stop it.  ⚠⚠ **The azimuth's wrong-way swing is REACHABLE with the shipped keys**: holding A is due west, azimuth **359.99…°**; adding S sends the vehicle to the odd-r south-west neighbour at −120°, azimuth **60°**.  The naive turn is **−300°** and the short one **+60°** — five sixths of a circle the wrong way, on one extra keypress, on a camera that looks correct throughout.  ⚠ 60° rather than the 45° the metre heading names, because `vehicle_facing` reads the velocity between hex CENTRES (`@X067`) | 2026-08-17 | `21-R2`, `@X070`, `@X071`, `12-B5b` |
| `@M025` | ⚠⚠ **loft's keyed collections are an ORDERED INDEX, not a bucket hash — so insertion order is not a hazard in this language, and the gate written to catch it could not fail.**  `hash<PaintedHex[q, r]>` iterated over six fixtures — reversed insertion, a scrambled spread `[17, 3, 999, −50, 40, 0, 7]`, a key landing mid-range, negative coordinates, and grow-then-erase — comes back sorted lexicographically **every time**, and stays sorted when an unrelated distant key is added.  ⚠ So plan 25 M2's headline test (*one map painted in two orders gives one mesh*) was aimed at a mechanism that does not exist; its own **control** is what said so, by finding the two fixtures iterate identically.  ⚠ `ground_chunk_mesh`'s coordinate walk stays for a different reason — the drawn region is wider than the painted set (`@X075`), so a walk over the entries cannot reach the coastline: a COVERAGE argument, not a determinism one, gated at **63 vertices against 0**.  ⚠⚠ The reusable half is the shape and it is `@M020`'s from the other end: *a gate aimed at the mechanism you EXPECT to be the hazard is not one aimed at the hazard*.  ⚠ Second reading of record: `mesh_crc` of an **empty mesh is 0**, so a mesher stubbed to emit nothing satisfies every equality in the file — every one carries a non-zero floor | 2026-08-17 | `25-M2`, `@X075`, `@M020`, `12-B3` |
| `@M026` | ⚠⚠ **a SHADER-written palette colour survives GL bit-exact — measured before plan 25 M3 was written, because R0 had only answered it for a BLIT.**  `probe/m3/` meshes a real world with the real mesher, draws it flat-unlit with the colour as a uniform through a real context under `xvfb`, captures with `gl_screenshot`, decodes with `imaging` and counts with `classify_canvas` itself: over **691 200 pixels**, `unknown` = **0** and drift = **0**.  ⚠ It is not R0 again — R0 measured `gl_upload_canvas` + `draw_texture_at`, pixels that were already 8-bit integers, where a fragment stage writes a FLOAT and can lose a bit to `GL_DITHER` (on by default in the spec), to an sRGB-encoding framebuffer, or to the driver's float→unorm8 rounding.  **One bit and M3's `other == 0` would not have been a gate that could be written at all.**  ⚠ The headless half generalises it: every one of the 256 byte values is a fixed point of `b/255 → round(f*255)`, so a palette entry added later is covered before it is drawn | 2026-08-17 | `25-M3`, `21-R0`, `@X074` |
| `@M027` | ⚠⚠ **EVERY per-kind count passes over a MIRRORED world; only a landmark sees it — at 490.8 px.**  `ground_mesh` mirrored in y, tops and sides together with the winding reversed to match, so the geometry is a clean reflection.  The GL gate then reported: *pixels that are not a palette colour* **0**, *background* **0**, *sea* **0**, grass **521 636** (true 520 408), wall **158 998** (true 160 118), wall_high **5 432** (true 5 489), rubble **2 297** (true 2 335) — **all green, all in band** — and `landmark sand at (-7, 7)` **490.8 px** from where `camera_screen` said it would be.  ⚠ That is `@X078`'s whole argument as a number, and it is `@M022`'s finding in a fifth place: a gate can read PERFECT while measuring the wrong thing.  ⚠ True landmark disagreement is **0.6 px / 0.5 px** against a 6 px tolerance, so the gate keeps ten times its headroom against the failure it exists for | 2026-08-17 | `25-M3`, `@X078`, `@M022`, `@X065` |
| `@M028` | ⚠⚠ **A ONE-HEX EDIT RE-BAKES ~4 000 HEXES' WORTH OF GEOMETRY, and the tile size is the only lever.**  `mesh_chunks_touched` names the edited hex's tile and its neighbours' and every one is re-meshed WHOLE, so a corner edit is four tiles of 1024 hexes to move one hex — **447 048 floats**.  Swept over `MESH_CHUNK_SHIFT`: worst-case edit **38.7 / 96.1 / 334.8 ms** and cold build **1.16 / 1.18 / 1.72 s** for 8x8 / 16x16 / **32x32 (shipped)**, over 192 / 48 / 16 tiles.  ⚠ The edit tracks tile AREA; the cold build barely moves and the BIGGEST tile is the slowest, because a large tile at the world's edge walks more hexes that are not drawn — so shrinking the tile is nearly free on the CPU side.  ⚠⚠ **It is not free on the GPU side** (12x the draw calls, and `GroupVboSet` has no frustum cull) and that half cannot be measured from `loft test`, so M4 measured the table and **changed nothing** — `plans/22`'s discipline, do not optimise against an unmeasured half.  ⚠ **Nothing pays this today**: M3 gave the ground a gate, not a window.  The deadlines it would face are `PAINT_DEBOUNCE_US` **50 ms** for a brush stroke and the tick's **667 ms** for a play-mode terrain change, so at 32x32 an edit is **7x over** a stroke — the number the phase that wires GL into `play_mode` inherits.  ⚠ Second reading: M2's parked one-pass rewrite is worth **7 %** (the redundant per-kind filter walks) and the other 93 % is mesh building the rewrite would still do — so it is NOT worth doing, and the gate is a trigger at 25 % | 2026-08-17 | `25-M4`, `25-M2`, `@X047` |
| `@M029` | ⚠⚠ **M1's two INVISIBLE breaks, priced for the first time: a zero-area sliver at every hex boundary TRIPLES the world's geometry and draws not one pixel.**  A solid tile of flat ground bakes **110 592 floats — exactly 108 a hex**, six triangles and no side quad.  Drop the `=` from `@X046`'s `hh <= nh`, or delete the guard entirely, and it bakes **331 776** — 3x — while the frame is pixel-identical; the whole world's cold build goes **1.29 s → 3.2 s**.  ⚠ `plans/25` § M1 called both *"invisible and pure cost"* in prose and gated them as COUNTS on four small fixtures; this is the same defect measured as VOLUME, and it is why a mesher wants a cost gate at all.  ⚠⚠ The instrument is the FLOAT COUNT and not the clock, because the clock could not carry it: two IDENTICAL back-to-back calls differed **5.4x** (1.84 s vs 0.34 s) when a registry fetch stalled inside one — a failure mode `11_f8`'s ratio never meets, because both its halves are `wave_tick` and neither touches the disk | 2026-08-17 | `25-M4`, `25-M1`, `@X046` |
| `@M030` | ⚠⚠ **EVERY BANKED MOVER HOLDS ITS RATE AT EVERY TIMESTEP, AND THE PLAYER DOES NOT** — `@D003`.  One simulated minute at **667 / 500 / 333 / 200 / 100 / 50 / 33 ms** ticks, every reading through the shipped release function: miner 1.0, robot 1.5, scout 2.5 and helper 2.5 hex/s come back **exact at all seven** (60 / 90 / 150 / 150), while the vehicle at 3.0 hex/s reads **180 / 120 / 180 / 0 / 0 / 0 / 0** against a true 180 and boosting at 6.0 reads **360 / 360 / 360 / 300 / 0 / 0 / 0** against 360.  `vehicle_hexes_this_tick` TRUNCATES and `Vehicle` has no `progress` field to carry a remainder in.  ⚠ End to end over a painted 40-hex corridor with a whole minute to cross it, the player **never leaves its starting hex** at 200 and 100 ms while a robot beside it arrives at all four — and at 500 ms it still arrives, at **two thirds** of its documented speed, which reads as feel rather than as arithmetic.  ⚠⚠ The consequence no rate table can express: at 200 ms a **boosting** player covers 300 hexes and a cruising one covers **none**, so a truncation does not scale a rate down, it **reorders which rates exist**.  ⚠⚠ Why no gate saw it — **three accidents in a row**: both shipped vehicle speeds are exact at the shipped tick (`3.0 × (1/1.5)` is 2.0 to the bit), the half-tick `23_k2a` already sweeps is exact too, and the one shortened timestep in the repo that WOULD see it (`23_k2a`'s tenth-tick, where `3.0 × 0.0667` truncates to zero) banks an **ENEMY**.  That is `11_f8`'s markerless-world trap with the axis and the subject swapped — **the right sweep over a roster with none of the broken thing in it** — and it is harder to see, because the sweep looks thorough.  ⚠ `@M013` cannot reach it from any direction: it varies the SPEED, at one tick length, through a mover that carries | 2026-08-17 | `26-L0`, `@D003`, `@M013`, `@M014`, `@X058`, `11-F8` |
| `@M031` | ⚠⚠ **THE PLAN'S OWN RECOMMENDED STEP MOVES SEVENTEEN TESTS, AND THE 654 MEASUREMENTS CANNOT SEE IT.**  `plans/26` § Open questions recommended a **666 667 µs** step as *"today's tick to within 1e-6 s"* that *"moves nothing"*.  Derived `TICK_SECONDS` from it and ran both gates: `scripts/validate.sh` **33 scripts / 654 measurements, all green and unmoved** — and `scripts/test.sh` **17 failures**.  `23_k2a`'s carry is no longer 0.0 to the bit (`0.000000500000000069889`), `3.0 * TICK_SECONDS` is `2.000001` rather than exactly 2, `19_p1`'s 1200-frame accumulation lands on 29 ticks instead of 30, and `19_p3`'s twelve seconds buys 17 ticks instead of 18.  ⚠⚠ **A step chosen to LOOK like the tick is not the tick**: `0.666667` and `0.6666666666666666` differ in the 7th digit, and a dozen assertions are pinned to the exactness of the second.  ⚠ The answer is a finer base unit — 1/3 µs, 3 000 000 a second, in which 2/3 of a second is the whole number **2 000 000** — and the derived `TICK_SECONDS` is then bit-identical, verified rather than assumed (`@X079`).  ⚠⚠ **The reusable half is which GATE saw it**: the scenario measurements are integer tick counts and a 5e-7 relative shift in the tick's LENGTH does not move one of them, so *the gate with 654 numbers in it was the blind one* and the unit tests pinned to bit-exactness were not.  A measurement's resolution is not its authority | 2026-08-17 | `26-L1`, `@X079`, `@M014`, `@M030` |
| `@M032` | ⚠⚠ **BOTH MOVER EPSILONS DELETED, `@D003` CLOSED, AND THE 654 MEASUREMENTS DO NOT MOVE.**  Plan 26 L2 replaced three hand-rolled *do-not-lose-a-fraction* sites (and the missing fourth) with one exact integer accumulator, gave the vehicle the bank it never had, and turned `ENEMY_PROGRESS_EPSILON` and `HELPER_PROGRESS_EPSILON` into deletions rather than zeroes.  Gates: `scripts/validate.sh` **33 scripts / 654 measurements, all green and unmoved**; `scripts/test.sh` **1269 green**.  The `@M030` sweep now reads **180 / 180 / 180 / 180 / 180 / 180 / 180** for the player and **360 × 7** boosting, against 180 / 120 / 180 / 0 / 0 / 0 / 0 and 360 / 360 / 360 / 300 / 0 / 0 / 0 before it — and the four L0 functions that pinned the wrong numbers went RED on the phase that changed them, which is the whole of what a tripwire buys.  ⚠ **Behaviour at the shipped tick is bit-identical by construction**: 3.0 hex/s over a 2 000 000-unit step releases exactly 2 hexes with nothing carried, so the 654 have nothing to see — the change is entirely in what happens at timesteps nobody ships yet.  ⚠⚠ **`@M017` is what makes the deletion a result rather than a tidy-up**: zeroing the float epsilon turned 7 of 1149 tests red and `scripts/validate.sh` with them, and the integer path needs no guard at all — `2.5 × (1/1.5)` summed three times is 4.999999999999999 in floats and `10 000 000 / 3 000 000` in units.  ⚠ `tests/23_k2a` keeps the three numbers the epsilon bought (40 / 80 / 60 hexes at 1.0 / 2.0 / 1.5 hex/s over 60 ticks) unchanged: *the same answers, with no constant producing them*.  ⚠ The one-shot TIMERS are untouched and still count float seconds — the family boundary held, and L3 is where it is tested | 2026-08-17 | `26-L2`, `@X080`, `@X081`, `@D003`, `@M030`, `@M017` |
| `@M033` | ⚠⚠ **THE THREE TIMERS WITH AN EPSILON HOLD AT EVERY TICK LENGTH; THE TWO WITHOUT ONE DO NOT** — `@D004`, and it inverts `plans/26` § 2, which counted the epsilons as the brittleness.  Swept through the shipped code at **667 / 500 / 333 / 200 / 100 / 50 / 33 ms** before any conversion: helper recovery 60 s (**90 / 120 / 180 / 300 / 600 / 1200 / 1800**), tower rebuild 20 s (**30 / 40 / 60 / 100 / 200 / 400 / 600**) and the 2 s boost (**3 / 4 / 6 / 10 / 20 / 40 / 60**) are all exact — while the wave lull reads **23 / 30 / 45 / 76 / 151 / 300 / 451** against a true 23 / 30 / 45 / **75** / **150** / 300 / **450**, and the 5 s pre-walk window reads **8 / 10 / 16 / 25 / 51 / 101 / 151** against 8 / 10 / **15** / 25 / **50** / **100** / **150**.  ⚠ Both unguarded ones are RIGHT at the shipped 667 ms, which is why nothing had seen it — `@M030`'s accident in the other family.  ⚠ Two of the three guarded timers count DOWN exactly as the broken pair does, so the DIRECTION is not the discriminator; **a guard is**.  ⚠⚠ **The second reading is about the GATE and is the reusable half.**  `plans/26` § L3 asks for *"a Timer counting UP to 20.0 s and one counting DOWN from it both fire on the same tick"*.  Measured at the shipped tick over six exact-multiple durations, float UP vs float DOWN vs the true count: 4.0 s **7 / 7 / 6**, 10.0 s **16 / 16 / 15**, 20.0 s **30 / 31 / 30**, 30.0 s **45 / 45 / 45**, 40.0 s **61 / 61 / 60**, 60.0 s **91 / 91 / 90** — so the two directions disagree at exactly ONE of six and **agree while both being a tick long at FOUR**.  *Two agreeing instruments are not a control; the TRUE count is.*  ⚠ Third reading: `scripts/test.sh` **1285 green**, `scripts/validate.sh` **33 scripts / 654 measurements green and UNMOVED** with all three timer epsilons deleted — the fourth phase running whose change the biggest gate cannot see, because every conversion is bit-identical at the tick dryopea ships | 2026-08-17 | `26-L3`, `@D004`, `@X082`, `@M030`, `@M022`, `15-C0`, `17-T1` |
| `@M034` | ⚠⚠ **THE COMPOSITION GATE THIS PLAN ASKED FOR CANNOT FAIL, AND ITS OWN CONTROL IS WHAT SAID SO.**  `plans/26` § L4 asks that *"a 1 Hz clock driven by a 30 Hz clock's ticks equals one driven from the wall"*.  Measured over 600 frames of 1/60 s, the two counts are equal after **every one of them** — and they must be, for any implementation that hands on whole fast steps, because 1 Hz is exactly thirty 30 Hz steps so every slow boundary falls ON a fast one.  ⚠ The property the row was reaching for — *a driven clock is as far along as the ticks that actually happened and never further* — is visible only when the steps do NOT divide: a 700 000-unit clock driven by a 300 000-unit one is behind at **3 of 21** frames (the 7th, 8th and 14th), **0** ahead, never more than **1** step behind, and level again at the common multiple.  ⚠⚠ **Third phase running where the stated gate is weaker than the phase's finding** (`@M031` in L1, `@M033` in L3) — and this one is the hardest to notice, because a vacuous gate PASSES.  ⚠ **Second reading, the cap**: over one frame stream with a 20 s stall in it, a dropping cap of 4 plays **4** ticks and a deferring one plays **24** and still owes 6, while both answer *fewer ticks* on the stalled frame — so the row's stated control (*equal counts would mean the cap did nothing*) passes for the wrong implementation and the deferring driver is what makes the gate able to fail.  Identical worlds per tick is asserted through `state_diff` against a counted control, both ways, plus the two capped/uncapped worlds DIFFERING so the pair cannot hold vacuously.  ⚠ **Third reading, the rate**: a wall minute at 1/3 speed is **30** ticks exact against **29** for the same scale truncated per frame — `@D003`'s shape arriving on a rate scaler instead of a mover.  ⚠ Gates: `scripts/test.sh` **1305 green** (20 new), `scripts/validate.sh` **33 scripts / 654 measurements green and UNMOVED** — the fifth phase running whose change the biggest gate cannot see, and here by design: nothing in dryopea consumes a cap, a rate or a composition | 2026-08-17 | `26-L4`, `@X083`, `@X084`, `@M031`, `@M033`, `@M022`, `@D003` |
| `@M035` | ⚠⚠ **AN EASED FOLLOW CAMERA DOES NOT REMOVE A LATTICE MOVER'S JUMP — IT MOVES IT ONTO THE MOVER, AND IT GROWS SIX TIMES DOING SO.**  Plan 26 L5, over 240 frames of one straight drive at 960x720, worst single-frame move in pixels: a raw follow camera leaves the mover at **0.0 px** and throws **14.918 px** at the ground; the shipped eased camera leaves the ground at **1.287 px** and throws **96.111 px** at the mover.  ⚠ **The one object an eased follow camera cannot smooth is the object it is following**, and `@M023` could not see it because it measured the camera's own target rather than anything drawn.  ⚠⚠ **Drawing the mover at alpha takes 96.1 px to 14.1 px and NO FURTHER** — `camera_rig_step` eases toward the mover's HEX, so a step-function target keeps a seventh alive; a camera following the **DRAWN** point reads **0.0 px** on the mover and **0.329 px** on the ground, beating the eased camera on the ground as well.  So `plans/26` § Open questions 6 answers *neither*: alpha is what the target ease was standing in for.  ⚠ **Second reading, the policies, priced with no camera in them at all** against the continuous ideal: interpolate 0.065 m worst frame and **2.598 m of lag for ever**; extrapolate 0.065 m cruising and **9.5e-16 m of lag** but **2.598 / 2.533 m** in one frame when the velocity starts or stops; ease-the-mover 0.252 m worst frame and 1.882 m of lag.  ⚠ **Third reading**: `CAMERA_EASE_RATE`'s own derivation promises a trail of **0.649 m** and the measured trail is **2.395 m**, because `v / k` assumes a continuous mover.  ⚠⚠ **FOURTH phase running where the stated gate is weaker than the phase's finding, and this one is off by ONE** — the row asks for *>200 of 240 frames* and the correct policy moves on **exactly 200**, because a mover that has not stepped yet has no previous position (`@M031` could not see, `@M033`'s control agreed for the wrong reason, `@M034` could not fail).  ⚠ And one control was not foreseen: pricing against `HEX_FLAT_TO_FLAT` (seven digits) rather than `lat_to_metres` (exact) charged extrapolation with **1.3 µm** that was the constant's — *a reference rounded to seven digits cannot certify a policy that is exact*.  Gates: `scripts/test.sh` **1322 green** (17 new), `scripts/validate.sh` **654 measurements green and UNMOVED** — the sixth phase running whose change the biggest gate cannot see | 2026-08-17 | `26-L5`, `@X085`, `@M023`, `@M034`, `@M022` |
| `@M036` | ⚠⚠ **THE PHASE'S OWN NEGATIVE CONTROL COULD NOT FIRE, AND THE FALSIFICATION THAT DID FIRE FOUND SOMETHING THE GATE LIST WOULD NOT HAVE.**  Plan 20 A1 asked for *"delete dryopea's `hex_body` dependency and A1's tests must fail to compile"*.  Measured over 18 tests: the shipped state **18 passed**; the `hex_body` line removed from `loft.toml` **18 passed**; that line AND its `[[package]]` block removed from `loft.lock` **18 passed**; `use hex_body;` removed from `src/part.loft` `Error: Undefined type Rig`.  `use <pkg>;` resolves an UNDECLARED registry package and rewrites the lock to match, so **a consumer can prove its IMPORT is load-bearing and can never prove its MANIFEST is** ([loft#968], filed; the third case after [loft#963] and [loft#966]).  ⚠⚠ **Second reading, and the reusable one**: ten deliberate breaks in `src/part.loft`, each firing on exactly its named test and nothing else — but break 4, replacing the cycle walk's PATH with a VISITED SET, left `test_a_part_that_contains_itself_is_refused` **GREEN** and was seen by the DIAMOND control alone.  *The test that catches a visited-set cycle check is not the cycle test; it is the legal catalogue nobody writes a test for.*  ⚠ That is `hex_part::cycle.loft`'s stated argument reproduced rather than quoted — the fifth phase running whose finding is stronger than its written gate (`@M031`, `@M033`, `@M034`, `@M035`).  Gates: `scripts/test.sh` **1340 green** (18 new, 95 files), `scripts/validate.sh` **33 scripts / 654 measurements green and UNMOVED** — the seventh phase whose change the biggest gate cannot see, and here by design: nothing draws a part yet | 2026-08-18 | `20-A1`, `@X087`, `@X001`, `@M035` |
| `@M037` | ⚠⚠ **NINE OF ELEVEN BREAKS FIRED, AND THE TWO THAT DID NOT ARE THE FINDING: BOTH GATES ASSERTED SOMETHING WEAKER THAN THEY CLAIMED.**  Plan 20 A3.  (1) *"the canopy hinges about a LATERAL axis"* asserted only `!bone_planar`, and swapping the axis to the planar `(0,0,1)` PASSED — because `bone_planar` is false whenever `oz` is non-zero and the canopy's 0.56 already made it so.  ***The assertion tested a consequence that a DIFFERENT field also satisfies.***  (2) *"the root's own offset is not part of any base"* could not fail, because every catalogue entry's root sits at `(0,0,0)` — **the fixture could not express the thing the guard is for**.  Both are now asserted directly (the axis by its components, the root by a part authored away from the origin) and both breaks fire.  ⚠ The nine that did fire include the phase's headline: one rotor moved 0.1 m outboard takes the width from **2.28** to **2.38** and names both numbers.  ⚠⚠ **Second reading, a loft CRASH**: calling `hex_body::rig_world_seg3` from a function in dryopea's LIBRARY SIGSEGVs the interpreter at `OpGetVectorNullable`, while the **byte-identical body in a program ENTRY answers correctly** — six narrowing probes each ran clean alone, so it is the whole function together ([loft#935]'s *size is an ingredient* shape).  Worked around by walking the parent chain, which at the neutral pose is what `rig_world_seg3` reduces to anyway.  Gates: `scripts/test.sh` **1351 green** (11 new, 96 files), `scripts/validate.sh` **654 measurements green and UNMOVED** — the eighth phase whose change the biggest gate cannot see, and by design: nothing draws a part yet | 2026-08-18 | `20-A3`, `@X088`, `@M036` |
| `@M038` | ⚠⚠ **A PHASE WAS DECLARED BLOCKED ON A BUG THAT A REBUILD HAD ALREADY FIXED, AND `loft --version` COULD NOT SAY SO.**  Plan 20 A2.  A measured part came back with **1 limb instead of 8** and the suite SIGSEGVed in `OpLengthVector`; it was real, deterministic, narrowed to ten lines and three calls, and filed ([loft#969]).  Re-tested against the binary actually on `PATH`: `/usr/local/bin/loft` (2026-08-16) **0 clean of 10**, `~/.local/bin/loft` (2026-08-18 11:45) **25 clean of 25** — and ***both report `loft 2026.8.0`***.  ⚠ The evidence had been gathered against a binary that was replaced two hours into the session.  ⚠⚠ **So the version string is not an instrument and the mtime is**: `docs/TOOLCHAIN.md` § The BINARY moves under you.  ⚠ **One correction inside the correction**: a first re-test read *2 of 10 wrong* on the new binary and that was a `grep` that could not tell *crashed* from *no match* — re-run with exit codes recorded it is 25/25.  *A blind instrument reports a symptom, not its cause.*  ⚠ The eight standalone ingredients that do NOT reproduce it are kept as a negative control rather than deleted, because the exclusions are the reusable half.  Gates: `scripts/test.sh` **1361 green** (10 new, 97 files), `scripts/validate.sh` **654 measurements green and UNMOVED** | 2026-08-18 | `20-A2`, `@X089`, `@M037` |
| `@M039` | ⚠⚠ **A DISC CANNOT SHOW THAT IT IS TURNING, AND THE GATE THAT WOULD HAVE SAID SO IS THE SILHOUETTE RATHER THAN THE CHECKSUM.**  Plan 20 A4.  `PARTS.md` § D7 gave the rotors a spin rate and called it *"the one place BOOST becomes visible without a HUD"* — and `ground_gl.loft` draws a part in ONE FLAT UNLIT COLOUR (`@X074`, which is what keeps a frame exactly classifiable), so a body of revolution turning about its own axis is a **still picture**.  Measured: a 12-segment disc at **1/12 turn** is its own silhouette to the bit (1.04 m both), and its worst case — half a segment — moves the width by **3.4 %**.  Two crossed blades at the same angle move it **9.4 %**, and both shapes read 1.04 m at rest, so the comparison is about turning.  ⚠⚠ **`mesh_crc` DOES see the disc turn**, because the vertices are permuted — *a checksum that moves while the picture does not is the wrong instrument*.  ⚠ So the ART changed and the FOOTPRINT did not: a blade spans the diameter the disc did, so 2.28 x 2.05 x 0.93 holds to the last decimal and A3's gate re-asserts it.  ⚠⚠ **Second reading, and it is a gate that could not fail**: `socket_world` was PLANAR (A1, when `hex_body` was) and the tower's socket is 6 m up (A3, `rig_bone3`), so it answered the tower's FOOT — while `tests/20_a3` asserted *where the top sits is derived* by taking the height from `rig_world_seg3` **directly** and asking `socket_world` only for x and y.  ***A gate that asks a different instrument for the interesting half is not asking at all.***  ⚠ Third reading, a loft defect between the catalogue and the renderer ([loft#974], filed, both backends, silent): a record fetched through an ACCESSOR reads its vector fields EMPTY after an unrelated allocation, so the second tower drawn has no limbs.  Both binaries checked before filing, which is `@M038` applied.  Gates: `scripts/test.sh` **1374 green** (13 new, 98 files), `scripts/validate.sh` **654 measurements green and UNMOVED** | 2026-08-18 | `20-A4`, `@X090`, `@X091`, `@X074`, `@M037` |
| `@M040` | ⚠⚠ **HALF OF EVERY BOX IN THE CATALOGUE WAS WOUND INWARDS, AND FOUR PHASES OF GATES COULD NOT SEE IT** (`@D005`).  Plan 20 A5.  `emit_box` picked its two in-plane axes as *right-handed about the outward one* and used **the same pair for a face and its opposite** — `y x z` is `+x` for the `+x` face AND for the `-x` face — so the three negative faces of every box wound the wrong way.  Measured: **72 of 144** triangles on the hover unit.  ⚠⚠ **It changed no count, no vertex position, no normal and no `mesh_crc`**: the quad's four corners are the same rectangle walked the other way, so even a fold over the triangle INDICES agrees.  Under the `GL_CULL_FACE` that `ground_gl.loft` turns on for the whole frame those triangles draw NOTHING — and A2/A3/A4 shipped 42 tests over them without a pixel existing.  The instrument is a cross product against the stored normal, swept over every catalogue entry, emitted turned and off the origin.  ⚠ **Second reading, and it moved two of `PROXY_ART.md`'s numbers**: distinctness is exact and says nothing about whether a PLAYER can tell two things apart, so `entity_min_distance2` was written as `palette_min_distance2`'s sibling — and the document's own two greys were the two worst pairs in the frame, **player `#f0f0f0` vs `waterfall` `#e8f4fc` at 224** and **crew `#c0c0c0` vs `rock` `#b0b0b0` at 768** (RGB distances of 15 and 28, which is *the same colour* on a screen).  ⚠ `PROXY_ART.md` picks its greys explicitly *distinct from terrain greys*, so its own CRITERION is what the numbers fail and `waterfall` is simply a kind it never checked: **the rule beat the number**, the floor is now **3264**, and a third number moved for a different reason — its tower top is `#d04848`, which is `palette_color(9)` to the bit and would have been counted as WALL.  ⚠ Gates: `scripts/test.sh` **1392 green** (18 new, 99 files), `scripts/validate.sh` **33 scripts / 654 measurements green and UNMOVED**, `scripts/validate_gl.sh` **3 fixtures / 52 measurements** (from 2 / 26) — and the entity draw is wired into ALL THREE GL fixtures, so `the-ground`'s and `an-island`'s `other == 0` now also say *nothing was drawn for a base with no roster*.  ⚠ The landmark reads **1.7 px** from where the camera says the player is | 2026-08-18 | `20-A5`, `@X092`, `@X093`, `@X094`, `@D005`, `@X074`, `@X078`, `@M039` |
| `@M041` | ⚠⚠ **THE DRAW-CALL COST PLAN 25 M4 WAS AFRAID OF IS ZERO, SO THE TILE SIZE IS DECIDED BY THE EDIT ALONE.**  Plan 19 P6, measured in a real GL window under `xvfb` — the half M4 said *cannot be measured from `loft test`*.  Ground + roster at 960x720, interpreted, as *tiles / one-hex terrain change / draw per frame*: **32x32** 4 / **54 ms** / **5 ms**, **16x16** 8 / **29 ms** / **5 ms**, **8x8** 24 / **7 ms** / **5 ms**; and on a world FOUR TIMES the area (5 073 hexes) **32x32** 8 / **112 ms** / **7 ms** against **8x8** **96 tiles** / **7 ms** / **7 ms**.  ⚠⚠ ***Twelve times the draw calls costs nothing measurable***, and the edit cost at 8x8 is CONSTANT in the world's size where at 32x32 it grows with it — so `@X096` shipped 8x8 and a body falling stopped being a visible hitch.  ⚠ Three other numbers from the same window: an idle play frame is **5 ms** (sync + draw), the roster's per-tick upload is **10 ms** (which is why it is gated on the TICK and not the frame — per frame it would triple a frame's cost to redraw a picture that cannot have changed), and entering play mode costs one **218 ms** cold bake.  ⚠⚠ **Second reading, and it is a total failure no gate in the repo could have seen**: `ground_gl_draw` and `entity_gl_draw` each turn depth testing and back-face culling ON and neither turns them off — right for them, and the editor's picture is a full-screen TEXTURE BLIT.  Measured: with the two `gl_disable` lines removed from `play_view_draw` the frame after a play frame is **691 200 black pixels of 691 200**; with them, a full editor frame.  *A player toggling P twice would see it and the GL gate could not*, because each fixture draws ONE frame and exits — so it is now a case of its own in `gl_gate.loft`, and it FIRED against the broken version.  ⚠⚠ **Third reading, and the reusable one: the new gate's first failure was its own bug.**  It filled a canvas with `GL_GATE_CLEAR`, a constant that carries **no alpha** because everything that reads it masks to 24 bits — so `create_painter_2d`'s blend drew a fully transparent quad and the check read a black frame for a reason that had nothing to do with depth testing.  ***A control that fires is not yet a control that fires for the right reason.***  ⚠⚠ **Fourth reading, from the suite TOTAL not reconciling**: `loft test` collects every zero-argument function in a test file AS A TEST, so a one-line `fn m2_edge() -> integer` helper made `25_m2` report 17 tests where it has 16 — a test that asserts nothing, counted by a green suite, in the number every plan's Status quotes.  Caught only because 1392 + 5 is not 1398.  ***A suite total that does not reconcile is a finding.***  ⚠ Gates: `scripts/test.sh` **1397 green** (5 new, 100 files), `scripts/validate.sh` **33 scripts / 654 measurements green and UNMOVED**, `scripts/validate_gl.sh` **3 fixtures / 53 measurements**. | 2026-08-18 | `19-P6`, `@X095`, `@X096`, `@M028`, `@M040` |
| `@M042` | ⚠⚠ **THE HUD COSTS NOTHING, AND BOTH WAYS IT COULD SILENTLY FAIL NOW HAVE A GATE THAT FIRES.**  Plan 19 P7.  A play frame with the overlay measures **5-6 ms** against 5 ms without it — the overlay is 114 x 40 pixels against a frame's 691 200, so it is built and uploaded EVERY frame rather than cached, which is the honest shape when the wallet drains continuously.  ⚠⚠ **A small PARTIALLY TRANSPARENT quad fails in two directions and neither is visible to a test** (`loft test` cannot blend, and the software `snap` composes no overlay): a fill with alpha 255 puts an opaque slab over the corner of the world, which looks deliberate; ink with alpha 0 draws nothing, which looks like a HUD nobody wired up.  Both are now cases in `gl_gate.loft` and both FIRED against their own break — the opaque fill read **0 of 3396** kept pixels, the alpha-less ink **0 of 1164** digits.  ⚠ The expectations are the overlay's OWN canvas rather than magic numbers: the blit is 1:1 at native size, so the ink on screen is exactly the ink in the canvas.  ⚠⚠ **Third reading, in a test helper**: the digit-shape checksum was `h * 131 + i` unbounded, and the overflow landed on exactly **0** for digit `0` — which the guard above it read as *drew no pixels at all*.  ***A checksum that can reach the value meaning "empty" is the wrong checksum***, which is `mesh_crc`'s `@X065` arriving one layer out.  ⚠ **Fourth, and it took the window to see**: the digits shipped at 14 x 24 and were legible only as a squint against a 960 x 720 frame, so they are 24 x 40.  A HUD is read at a glance or it is not read, and no headless gate can say which.  ⚠ The digit TABLE is gated by an INDEPENDENT ORACLE — the lit-segment counts **6 2 5 5 4 5 6 3 7 6**, which are a property of the shapes and not of this repo — plus a position-sensitive shape fold, because the counts cannot tell a `2` from a `3` or a `5` from a `6`.  ⚠ Gates: `scripts/test.sh` **1405 green** (8 new, 101 files), `scripts/validate.sh` **33 scripts / 654 measurements green and UNMOVED**, `scripts/validate_gl.sh` **3 fixtures / 55 measurements**. | 2026-08-18 | `19-P7`, `@X097`, `@M041`, `@X065`, `@X092` |
| `@M043` | ⚠⚠ **A RAMP IS NOT A COLOUR, IT IS 201 — AND ONE OF THEM WAS A ROBOT'S.**  Plan 19 P7, the wallet ramp.  Swept over every whole point the wallet can show, amber `#ffc000` to red `#ff0000` passes through **`#ff8000` at exactly 134 points**, which was the SCOUT's colour: at one value of the wallet the HUD would have been painted in a robot's colour, and `gl_gate.loft` counts by EXACT colour, so those pixels would have been counted as a robot.  *Shares slightly off* — the failure `PARTS.md` § D3 says a frame gate cannot see.  ⚠ The scout moved to `#a0e000` and the ramp's worst approach to anything a frame can hold is now **3969** squared-RGB (against `builder`), above the entity table's own 3264 floor; the scout's own nearest neighbour is 5120.  ⚠ Its drawn pixel count is unchanged at **4687** — same geometry, different colour — so the GL band did not move.  ⚠⚠ **The sweep is exhaustive rather than sampled ONLY because `@X098` made the ramp a function of the integer the digits show.**  Over `wallet_left` it would reach uncountably many colours and no test could do better than spot-check — *and the spot would have been 200 and 0, which are both clean.*  ⚠ **It fired twice, and the second time on this session's own incomplete edit**: the scout move was written into a file that a failed assertion earlier in the same script had prevented from being saved, and the sweep named `scout at 134 points` again.  A gate that can see a defect can also see a botched fix.  ⚠ **Third reading, a top byte again**: `hud_ink_for` builds through `rgb()` and so carries ALPHA 255, while `HUD_INK_FULL` is the bare 24 bits — comparing them raw reads **4294950912 against 16760832**, and every endpoint assertion fails for a reason that is not the ramp.  The same top byte cost `gl_gate.loft` a false failure in `@M041`, one layer out.  ⚠ Gates: `scripts/test.sh` **1407 green** (10 new, 101 files), `scripts/validate.sh` **33 scripts / 654 measurements green and UNMOVED**, `scripts/validate_gl.sh` **3 fixtures / 55 measurements**. | 2026-08-18 | `19-P7`, `@X098`, `@X097`, `@X092`, `@M040`, `@M041` |

---

## How to use this

```bash
grep -rn '@X025' .          # everywhere the permit decision is cited
grep -rn '@M001' .          # everywhere the 75% figure is quoted
grep -rn '@X0[0-9][0-9]' docs/ | wc -l
```

⚠ **Adding a row is cheap and removing one is not.**  A code is permanent even
after the decision is reversed — a reversed decision keeps its code and gains a
`⚠ SUPERSEDED by @X###` line, exactly as `PROBLEMS.md` keeps `@D001` after the
fix.  That is what makes an old commit message still resolvable.

⚠ **This index does not cover the older docs yet** — `DESIGN.md`, `SETTING.md`,
`ENEMY_MOVEMENT.md`, `ROBOT_ECONOMY.md` and `GROUND_TYPES.md` carry many
decisions with no codes.  Backfilling them is worth doing when something needs
to cite one, not as a sweep: a code nobody references is a code nobody
maintains.
