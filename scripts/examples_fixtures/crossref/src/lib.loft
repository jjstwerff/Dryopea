// A consumer citing a REGISTERED LIBRARY's worked example rather than
// copying it.  @TST-001 is defined in `ok/tests`, nowhere here.
//
// Example: @TST-001
pub fn wraps_add(a: integer, b: integer) -> integer { add(a, b) }
